# floor2 — คิวการตัดสินใจของ owner (สิ่งเดียวที่ค้างก่อนปิด stage-03)

**Date:** 2026-07-11 · **ทำไมมีไฟล์นี้:** ทั้ง benchmark (Structured3D + FloorPlanCAD) และแผ่นจริง
(floor2 v4/v5) ถูกวัดจนสุดชั้นที่เครื่องทำได้แล้ว — เหลือแต่ชั้นที่ต้องให้ owner เซ็น (กฎสองชั้น:
geometry เครื่องแก้, boundary/furniture owner เซ็น). ตอบ 5–7 ข้อนี้รอบเดียว = ปิด stage-03 floor2 ได้.
รวมของค้างจาก **v4 gate (defect-report.md)** + **v5 flags** ไว้ที่เดียว. เครื่อง **เสนอเท่านั้น**
(PROPOSAL-ONLY) — ไม่มีอะไรแตะ scene-graph จนกว่า owner เซ็น.

---

## A. v4 gate — ✅ ปิดครบแล้ว 2026-07-11 (owner ตอบ 5/5)

**สถานะ gate ล่าสุด: verdict = PASS — FAIL 0 / REVIEW 0 / signed 5, answer key 8/8.** ทุกข้อที่ owner
ตอบถูก apply + รัน gate ยืนยันแล้ว. `defect-gate.json` verdict PASS.

- ✅ **ข้อ 4+5 (ช่องห้องน้ำใหญ่ 1.15ม.)** — owner: "ก" = **หน้าต่างห้องน้ำ** → declare `w6` (window, rect
  [-47,7319.8,55,8471.9] ผนังตะวันตก ชุดเดียวกับ w3/w4) ใน `openings-v4.json`. เคลียร์ทั้ง
  C3_undeclared_doorway + C3_swing_obstructed (ฝักบัว) เพราะหน้าต่างไม่มีวงสวิง. geometry ยืนยันแล้ว
  (w6 → sill+glass+lintel 3 กล่อง, s2 → ช่องเปิด 0 กล่อง; รวม 24 opening boxes). sill 900/head 2200 = render default.

**เหลือ (ไม่บล็อก gate):** 3D re-render — ผมแก้ manifest/scene-graph/openings ไปแล้ว placement-gate marker
เลย stale ต้อง re-gate (read-vs-sheet) + build_floor ใน Blender ก่อน. **การ render = ask-tier (งาน GPU)** ตาม
pipeline/CLAUDE.md → บอก "render" เมื่อพร้อม ผมทำรอบเดียว (table เล็กลง + s2 เป็นช่องเปิด + w6 หน้าต่าง).

---

### (ประวัติ) รอบแรก 2026-07-11 — owner ตอบ 5 ข้อ, gate REVIEW 5→2 ก่อนปิด

**สถานะ gate ตอนนั้น:** FAIL 0 / **REVIEW 2** / signed 5 (ลดจาก REVIEW 5), answer key 8/8.

- ✅ **ข้อ 1+2 (โต๊ะกลม∩เก้าอี้ tub ×2)** — owner: "ไม่ได้ตั้งใจซ้อน" → **แก้แล้ว:** ย่อโต๊ะกลม
  Ø600→Ø500 (center คงที่ 6914,1414) เคลียร์ทั้งสองข้าง (ตรวจด้วย SAT ของ gate). ทางเลือกที่ไม่ได้ใช้ =
  คงØ600 + กางเก้าอี้ออกข้างละ 40mm — ถ้าอยากได้โต๊ะ 600 เท่าเดิม บอกได้ ผมสลับให้. `scene-graph.sitting_room.json` (manual_edit).
- ✅ **ข้อ 3 (s2)** — owner: "ไม่ใช่บานเลื่อน" → **แก้แล้ว:** reclassify `sliding`→`opening` (ช่องเปิดเปล่า
  ไม่มีบาน, ไม่มี swing), ลบ owner_confirm_pending, อัปเดต answer-key ที่เคยเขียน "บานเลื่อน". `openings-v4.json`.
  - ค้าง 1 คำถามยืนยัน identity (ตอบเมื่อสะดวก): s2 เป็น **ช่องเปิดเปล่า** / **บานเปิดคู่** / **กำแพงทึบ**?
    ระหว่างนี้ render เป็นช่องเปิด (ปลอดภัยสุด เติมบานทีหลังง่าย).

- 🔴 **ข้อ 4+5 = คำถามเดียว** (`fp 6bbc01f99270` + `6ab908d19743`) — **ดูรูป `v4/review-ensuite-gap.png`**
  ตรงผนัง**ตะวันตกของห้องน้ำใหญ่ (ที่มีฝักบัว)** มีช่องกว้าง **~1.15 เมตร** (แดงในรูป). เป็นผนังเส้นเดียวกับ
  w3/w4 ที่พี่เคยบอกว่าเป็น "หน้าต่าง" แต่ช่องนี้กว้างกว่าและอยู่ติดฝักบัว. **มันคืออะไร?**
  - (ก) **หน้าต่างห้องน้ำ** → ผม declare เป็น window; เรื่องฝักบัวชนประตูหายไปเอง (หน้าต่างไม่มี swing). ← เดาว่าน่าจะข้อนี้
  - (ข) **ประตูห้องน้ำ** → บานเปิดเข้าทางไหน? (ถ้าเปิดเข้าห้องน้ำจะชนฝักบัว — ต้องเป็นบานเลื่อน/เปิดออก)
  - (ค) **ช่องเปิดเปล่า** (ไม่มีบาน)
  - (ง) **จริง ๆ ต้องเป็นกำแพงทึบ** (build หายไป → ผมเติมกำแพง)

> **หมายเหตุ:** ข้อ [SIGNED] 5 รายการ (ตู้ BF09 นอก outline ×2 + ขอบห้องไม่มีกำแพง ×3, transcribed จาก
> log owner 07-05/06/07) — ไม่ต้องทำอะไรเว้นแต่อยากถอนลายเซ็น.

---

## B. v5 (สาย BLIND) — ของค้างเชิงนิยาม (ไม่บล็อก v4; เป็น lane วัดผล)

v5 คือการ re-derive floor2 แบบไม่ดูของเก่า เพื่อวัดว่าเครื่องอ่านเองได้แค่ไหน — **วัดเสร็จแล้ว**
(กำแพงหนา 100% ตรง v4, boundary recall 84%/precision 6.8%, furniture อ่านไม่ออก). ของค้าง = ตัวเลือก,
ไม่ใช่งานที่ต้องรีบ:

6. **tg1 / tg2** (`v5/floor2_v5-thin-glass-flags.json`) — กระจกบานเลื่อน master→ระเบียง + เส้นขอบระเบียง.
   สถานะ OWNER-CONFIRM-PENDING (machine-inert, ยังไม่เข้ากำแพง). = เรื่องเดียวกับที่ v4 เซ็นไปแล้วในชั้น
   manual_additions; v5 แค่ยืนยันว่า blind read หาเส้น indoor/outdoor หลักเจอเอง (แต่ pier/party wall
   ละเอียดหาไม่เจอ). **ไม่ต้องทำอะไร** เว้นแต่จะใช้ v5 แทน v4.

7. **v5 furniture scene-graph** — ยังไม่ทำ (เฟอร์นิเจอร์เป็นชั้น owner-authored: เครื่อง cluster ไม่ออก
   เพราะเตียง+หัวเตียง+ตู้ชิดกันเป็นก้อนเดียว). **ข้อเสนอ:** ถ้าต้องการปิด stage-03 บนสาย v5 ด้วย ผมร่าง
   ตำแหน่งเฟอร์นิเจอร์ master suite จากตำแหน่งที่วาดในแบบให้ (เป็น candidate) → รัน `placement_gate` +
   `suite_clearance` → owner เซ็นทีละชิ้น. **บอกมาว่าจะเอา** (แต่ v4 มี scene-graph ที่ใช้ได้อยู่แล้ว —
   v5 furniture เป็น optional).

---

## วิธีปิดให้จบรอบเดียว
- ตอบข้อ 1–5 (แค่พิมพ์ตอบสั้น ๆ ก็ได้ ผมเซ็น fingerprint ให้ในไฟล์ + แก้ geometry ตามที่ตกลง แล้วรัน gate ซ้ำ).
- ข้อ 6–7 ตอบเมื่ออยากได้ (ไม่บล็อก).
- gate ผ่าน FAIL 0 + ไม่มี REVIEW ค้าง → floor2 พร้อม build / render.
