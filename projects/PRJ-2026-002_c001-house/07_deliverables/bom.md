# BOM · PRJ-2026-002 — NOT GENERATED (recorded per contract)
Contract rule: BOM is produced ONLY via the human-triggered `bom-generate` skill.
It was not triggered this run, and correctly so:
1. Scene FF&E are client-program placeholder dimensions (BF basis), not catalog
   SKUs — an `ffe-research` pass (ffe-candidates.json) must land first.
2. SHOWCASE under best-case override A3 (no budget) — costing has no anchor.
When a real engagement starts: run ffe-research → human triggers bom-generate →
regenerate this file. Licensing note for the render itself: see licensing-check.md.
