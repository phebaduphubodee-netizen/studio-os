# DELIV-001 — P2 round 24 — 2026-08-12 — gate #23 — de-periodised slack + swing clamp: ทะลุกันหายจากตารางกรรมการทั้งสองราย, คาบชายผ้าวัดได้ลด 0.591→0.434 (ยังไม่ตาย — ฟันคือ cell และ throw ไม่เคยได้ hem cue), C2 ยก "แสงทั้งห้อง" ขึ้น #1 ตรงกับหัวคิว P4

**ภาพ:** ก่อน `pipeline/output/room_bedroom_suite_eye_p2r23.png` | หลัง `..._p2r24.png`
(A/B สามธง ไม่ต้องแก้ source: `--no-slack-waves` = slack สม่ำเสมอแบบ r23 ·
`--no-swing-clamp` = swing แบบ r23 · `--slack-waves=<d>` = bisect ความลึก field)

## 1. กลไก — สองกลไก ทุกตัวมีเลขก่อนสร้าง

- **De-periodised slack field** (`softgoods.slack_field` + `modulate_slack`):
  กรรมการทั้งสองตั้งชื่อไซต์เอง (gate #22): คลื่นชาย = periodicity ไม่ใช่ amplitude
  → modulate rest-length excess ของ throw (fall 0.30 สองทาง, band บนเตียง 1.0
  dip เดียว) + coverlet skirt ด้วยคลื่นระนาบ 3 ทิศ/ความยาวคลื่น incommensurate
  (1.13/0.47/0.23 ม. — ทุกตัวเหนือ fold pitch: field คุม "ที่ไหน" solver ยังเป็น
  ผู้เขียน wrinkle คนเดียว, กฎ enter-smooth ไม่แตะ) · duvet ไม่แตะ (standing-fold
  preset คุ้มครองใน drape.py) · vault ยืนยันทิศก่อนสร้าง: "fold จริงวิ่งเป็น
  structured families ตามแนว tension ไม่ใช่ noise" (trn002-reference-study)
- **Garment-swing collision clamp** (`build_room` swing block +
  `softgoods.rot_aabb_half`/`aabb_penetration`): claim r23 วัดยืนยัน 36-42มม.
  สามคู่ → ลำดับแก้คือฟิสิกส์ราวจริง: **slide ตามราง** ก่อน (capped ใน slot,
  D-029) แล้วค่อยลดมุม; piece ที่หมุนไม่ได้อยู่นิ่ง (D-031) · allowance 12มม. =
  ค่า graze เดิมของ bay check · วัด/พิมพ์ด้วย instrument เดียวกับที่ยืนยัน claim
  ทุก build — residual ซ่อนไม่ได้
- tests ใหม่ 9 (identity/bounds/determinism ×3 · **aperiodicity ที่ fail ได้**:
  sine control >0.95, field <0.85 · refuse stretch · clamp math ×4) — suite
  รวม **181 เขียว**

## 2. ผล full

- scorecard **9 PASS / D7 FAIL(P4) — QUALIFIES** · D5 10.5609 in band ·
  GPU render ยืนยัน (ไม่มี silent CPU fallback)
- harness ครบ: duvet **1.827x (จุดเฝ้า: ขยับจาก 1.762** — coverlet ใต้มัน
  re-solve ด้วย field ใหม่ duvet จึง settle ต่างเดิม; rung นี้ตัดที่ critic clause
  ไม่ใช่ตัวเลข) · throw 1.075x นิ่ง · wood 0.0 · rug ROLLED 5.0px · shells 0
- **swing clamp ในเฟรม:** slide จริง 4-171มม. ต่อราง · คู่เทา+ดำช่องสองที่ r23
  merge เป็นก้อนเดียว ตอนนี้แยกเป็นสองตัว เห็น silhouette ครบ · เลข residual
  ที่พิมพ์: max penetration 55-115มม. ต่อราง = **over-read ของ AABB บนเสื้อ
  layered ขนาน** (R9b: AABB แยก interlocking จาก intersecting ไม่ได้) — เสื้อ
  ชุดนี้ stack ตามแกน depth ของ carcass, slide แกนรางจึงกดตัวเลขได้บางส่วน
  แต่ผลตาม LOOK และตามกรรมการคือแยกจริง
- **ชายผ้าวัดตรงเส้นฟัน:** autocorr max **0.591 (r23) → 0.434 (r24)** และ lag
  เด่นแตก (115→52px) — คาบลดจริงแต่ยังเหนือพื้น aperiodic · **สาเหตุที่เหลือระบุ
  ได้จาก crop:** pitch ฟัน ≈ ขนาด cell (0.022ม.) = LINEAR buckle ที่ความยาวคลื่น
  สั้นสุดที่ mesh แทนได้ และ **throw ไม่เคยได้ hem cue** (ชายเย็บพับสองชั้น =
  แข็งกว่า = buckle ยาวกว่า) ที่ coverlet ได้ไปแล้วตั้งแต่ p2r19 — กลไกรอบหน้า
  มีชื่อจากฟิสิกส์ ไม่ใช่จากการหมุน knob เดิม (R1: ไม่ใช่ fix-shape ซ้ำ)
- เสื้อดำ (จุดเฝ้า sheen_rough): p50 5.0 / p95 50 (r23: 6.0/51) — ไม่มี vinyl
  regression, ความลึกคือแสงในช่อง (คิว P4 ตามเดิม)

## 3. กรรมการ (archive + index ครบก่อน triage)

**C2 (blind): 13 ข้อ — #1 เปลี่ยนเป็น "แสงทั้งห้อง" ครั้งแรก** (แถบทแยงไร้แหล่ง
บนผนังระแนง + sconce ทุกดวงดับ + ไร้แสงธรรมชาติ — ตรงกับ build-order law ของ
เจ้าของและหัวคิว P4 ที่ re-rank ไว้ที่ gate #21) · ผ้าคลุมตก **#2** · **ไม่ไฟล์
ทะลุกัน** · **C3 (pro): 7 ข้อ — ไม่ไฟล์ทะลุกันเช่นกัน** (รอบแรกนับแต่ claim เกิด
ที่ทั้งสองรายเงียบเรื่อง interpenetration) · C3 top-3 surface 3/3 (ordering
variance — แสงตก #4-5)

| ข้อ | triage |
|-----|--------|
| C2#1 แสงทั้งห้อง + C2#9 sconce ดับ + C3#4 slat มืดตัน + C3#5 contact shadow | **accept → P4/lighting หัวคิว** — C2 pick-one คือแสงครั้งแรก = เสียงกรรมการ blind มาบรรจบ build-order law เอง (หลักฐานสะสม 5 รอบ ทุกกรรมการ) · slat: pixel-diff p99=1 = refile บนภาพที่ไม่เปลี่ยน (protocol) + D2-A backer signed ยืน |
| C2#2 (1) ชายฟันเลื่อย | จ่ายครึ่ง มีเลข: 0.591→0.434 · ไซต์ยืนใน P2r-5 · กลไกหน้า: **throw hem cue** (hem_verts/factor 2.0 — เครื่องมี, throw ไม่เคยได้) |
| C2#2 (2) ขอบบนไม้บรรทัด + C3#1 "โค้งสม่ำเสมอ" | instrument B (RMS ต่อ quadratic fit) แยก r23/r24 ไม่ออก (23.6 vs 24.4px — ขอบ multivalued เพราะ fold พาด) — **ตาชนะ: ไซต์ยืน** · lever ถัดไป: bisect `--slack-waves` + salt_amp ขอบหัว (0.012→ใต้เพดาน 0.045) |
| C2#2 (3) ผิวเรียบ + C2#7 เนื้อผ้าไม่เท่าพรม | คลาสเดียวกับ P2r-5 · ครึ่งหนึ่ง = thread-pitch minification (probe 2026-07-22: map ที่ pitch ผ้าจริง ย่อจนแบนที่ระยะห้อง — เพดานฟิสิกส์ที่วัดแล้ว) · ครึ่งแสงแบน → P4 |
| C2#3 + C3 styling ว่าง | D7 7 vs 12 → P4 ตามบาร์ (record ยืน) · "เสื้อซ้ำทรงเดิม" = set เดียว repeat (D-029 law) — variety = P4 craft |
| C2#4 โคม/โต๊ะซ้ายหาย | rest_on 0.000 ยืน · **โต๊ะข้างเตียงซ้ายมีจริงใน spec** (บังด้วยเตียงที่มุมกล้องนี้ — occlusion ไม่ใช่ absence) · composition คิวตาพี่ (รอบ 6) |
| C2#5 microcement + C3#6-r23 handleless | identity = D6-A signed ยืน (push-open, reveal วัดแล้ว r18) · **ครึ่งใหม่มีเลข: |lap| 0.86× ของ oak ข้างกัน** — ทิศถูกแต่ "ชัดเจน" เกินเลข · accept เล็ก → P4 accent-map craft |
| C2#6 หัวเตียงเตี้ย | D-034 ยืน (สูง 1.10ม. = declared assumption, แดนตาพี่ 12 รอบ) · contact shadow ที่รอยต่อ → P4 |
| C2#8 + C3#2 เสื้อดำ/เสื้อแข็ง | stiffness = D-031 ยืน · ดำ: p50 5.0 ≈ r23 (6.0) — **C3 พลิกคำ ("ไวนิล"→"ไม่สะท้อน") บนภาพที่ตอบแสงเท่าเดิม = wording variance** · ช่องลึกแสงแบน → P4 |
| C2#10 กองผ้าโฟม | ชั้นวาง: pixel-diff **p99=1 = refile ของ item ที่ปิดแล้ว** (gate #20, folded_knit จาก reference I-24-062 — protocol ยืนยันกรรมการเปลี่ยนไม่ใช่ภาพ) · ม้านั่ง: ไซต์ยืนใน P2r-5 (มุมกล้องนี้) — ไม่แตะจนกว่าจะมีกลไกใหม่ (R1: crease_wander ซ้ำ = halt) |
| C2#11 ร่องดำหัวตู้ | accept-with-check → P4 craft (carcass-ceiling scribe/filler) — ยังไม่มีเลข, ต้องเปิด .blend ดูว่า seam ระหว่าง tower หรือ geometry จริง |
| C2#12 มวลอ่านไม่ออก ×3 | (i) กองขวา = โต๊ะข้างเตียง+cube (record r23) (ii) แผ่นซ้าย = กระจก jewel niche (ปิดที่ gate #21 — ขอบเฟรมตัด) (iii) ก้อนขาว = bulkhead (record r15, "ถ้าเป็นแอร์" → P4 identity cue) — ทั้งสามมี record, ไม่มี geometry ใหม่ |
| C2#13 จุลภาค (หนังสือ/พรมไม่ยุบ) | R9b advisory family + crush lane p2r16 — record, P4 craft |
| C3#3 ไม้ tiling | **refuted ซ้ำ + protocol: ไม้ล้วน p99=1** (autocorr 0.0 รอบ 11/13 ยืน — ค่า diff สูงในตู้มาจากเสื้อที่ slide จริง) |
| C3#7 ขอบไม้คม | D-043 ยืน (relaned: arris highlight มีจริงตรงแสงมีทิศ ศูนย์ตรงแสงแบน → P4/lighting) |
| C3#1 หมอนพอง | แดน decided ของตาพี่ (no-dent ×2) — escalate ยืน, builder ไม่แตะ |

**ไม่มีข้อไหนถูกทิ้ง** — accept+lane หรือหักล้างด้วยเลข/ink/record/decided (R7)

**Judge clause (บาร์ reopened ข้อ 3):** C2 top-3 surface **1/3** (#2 เท่านั้น —
ครั้งแรกที่ #1 ของ C2 ไม่ใช่ surface) · C3 top-3 surface 3/3 (ordering variance;
ใน 3 ข้อ: tiling = refuted+protocol, หมอน = decided, เหลือ accepted จริงคือคลาส
ผ้า) · **accepted-after-triage surface = 2** (P2r-5 residual คลาสเดียว: ฟันชาย+
ขอบ+ม้านั่ง+ผิว · microcement blur 0.86) **≤2 — รอบที่ 10 ติด** ·
**garment-swing หลุดจากตาราง accepted: กรรมการทั้งสองรายเงียบเรื่องทะลุกัน
รอบแรก** · C3 raw: …→5→6→**7**

## 4. DR consideration (คำสั่งเปิดทางของเจ้าของ session นี้)

เช็คเกณฑ์ DR-initiation ทั้งสี่: ไม่มี R1 stop ที่เป็น mystery (กรรมการตั้งชื่อ
กลไกให้แล้ว) · ไม่มี defect class เข้ารอบสองแบบไร้ทิศ (hem cue = กลไกใหม่มีชื่อ
จากฟิสิกส์+เครื่องที่มีอยู่) · vault ถามก่อนสร้าง (knowledge-manager): ยืนยันทิศ
field + พบช่องว่างเดียวคือ garment-spacing ซึ่งไม่ load-bearing (clamp derive
จาก AABB ที่วัดจริง) → **ไม่ยิง DR ใหม่รอบนี้ — consume ให้หมดสำคัญกว่า initiate**
(สถานะ subscription: 39/61 unit write-only คือแผลที่ต้องไม่ถ่างเพิ่ม)

## 5. ตัดสิน (R3 — ผมปิดรอบ)

- **P2r-5 ไม่ปิด — จ่ายครึ่งอย่างวัดได้:** swing clamp จ่ายเต็ม (claim หายจาก
  กรรมการอิสระทั้งสองราย) · sine-hem จ่ายครึ่ง (0.591→0.434) และเหลือครึ่งที่
  **ชื่อกลไกชัดกว่าเดิม**: (1) throw hem cue — ฟันคือ cell-scale buckle ของชาย
  ที่ไม่มีตะเข็บ (2) ขอบหน้า: bisect depth + salt_amp ใต้เพดาน MAX_HEM_WANDER
  (3) ม้านั่ง: รอกลไกใหม่ ไม่หมุน crease_wander ซ้ำ
- **โครงสร้าง top-3 เปลี่ยนข้าง:** C2 ยกแสงขึ้น #1 ด้วยเสียง blind ของตัวเอง —
  ตรงกับหัวคิว P4 ที่ re-rank ไว้ gate #21 และ build-order law ของพี่ · ไซต์
  surface ที่เหลือใน top-3 ของทั้งสองราย = คลาสผ้า P2r-5 (มีชื่อ+เลขครบ) กับ
  แดน decided (หมอน/D-031)
- **Escalate สู่ตาพี่ (ไม่ block):** หมอน (C3#1 ซ้ำ — no-dent เป็นคำพี่) ·
  D-034 หัวเตียง (12 รอบ) · composition โคม/ซอก (รอบ 6) · microcement identity
  (D6-A ยืนแต่โดนสองรายสองคืนติด) · Q1 · Q2 · jewel/ช่องแสง
- duvet watch 1.827x — เฝ้าต่อ ไม่แตะ

**Reversible:** `--no-slack-waves` · `--no-swing-clamp` · หรือ revert commit
เดียว — ไม่มี signed value ใดถูกแตะ, ไม่มี decision row ใหม่ (กลไก builder-side
ล้วน มีธง A/B เป็นทางย้อน)

**Spend รอบนี้:** quick ×1 · full ×1 · C2 ×1 · C3 pro ×1 (+1 retry หลัง 503;
วันนี้ pro 4) · DR ×0 (vault ask ×1, consumed ในรอบ) · tests 181 เขียว ·
commit ตามหลัง gate นี้
