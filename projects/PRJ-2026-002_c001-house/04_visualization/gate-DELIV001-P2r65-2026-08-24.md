# DELIV-001 / STY-7 + STY-8 — 2026-08-24 — gate #65

**ภาพ:** ก่อน `pipeline/output/room_bedroom_suite_eye_p2r66qA_ql.png` (ขา A — legacy 1.4118x)
| หลัง `pipeline/output/room_bedroom_suite_eye_p2r65q_ql.png` (ขา B — asserted 1.0000x)
คู่นี้เป็น playblast ทั้งคู่ เรนเดอร์ด้วยค่าเดียวกันทุกอย่าง ต่างกันตัวแปรเดียว
(`--floor-tile-legacy`) — **ยังไม่ปิด gate** เพราะยังไม่ใช่ full fidelity

1. **แก้อะไร:** สเกลของเท็กซ์เจอร์กลายเป็นสิ่งที่ต้อง **assert ตอน ingest** เหมือน mesh
   (R8 มีฝั่งโมเดลมาตั้งแต่ 08-01 ฝั่งเท็กซ์เจอร์ไม่เคยมี) — sidecar 16 ชุด + ประตูเดียว
   `_tile_m_for` + ทะเบียน `qa/texture-scale.json` + rung ที่บล็อกใน `check_room` + 30 เทสต์;
   และพื้นห้องซึ่งเป็น **ผิวเดียวในเฟรมที่ไม่มีใคร assert** เปลี่ยนจากเลขที่พิมพ์ไว้ 2.4 ม.
   เป็นขนาดที่ผู้เผยแพร่ประกาศ 1.6999997 ม.
2. **ผมตัดสินว่า: PASS ครึ่งเดียว — กลไกผ่าน, พื้นผ่าน, และผมไม่แตะสิ่งอื่นที่วัดเจอ.**
   พิสูจน์ในไฟล์ที่สร้างจริง ไม่ใช่จากซอร์ส: อ่าน UV ออกจาก .blend ทั้งสองใบ →
   ก่อน `2.400000 ม.` หลัง `1.700000 ม.` (ขา A ที่รันด้วยตัวคูณ 1.4118 อ่านได้ 2.400060
   คลาด 6e-5). ทางที่ทิ้ง: **ไม่แก้พรม** (วัดได้ 4.81x ซึ่งแรงกว่าพื้น) เพราะเป็นคนละผิว
   คนละผลลัพธ์ และ C2 ยื่นข้อพรมไว้ที่ p2r64 โดยที่ยังไม่มีใครทดสอบว่าสองเรื่องนี้
   เกี่ยวกันไหม — ลงเป็น **แถวหนี้ (debt) ที่พิมพ์ทุกรอบ** ไม่ใช่ความเงียบ
3. **ไม่แน่ใจ:** **ราคาที่ผมทำนายไว้เองแล้ววัดไม่เจอ.** เอกสารของรอบนี้เขียนไว้ก่อนว่า
   ที่ 2.40 ม. พื้นที่มองเห็น "ซ้ำ 0.0%" และที่ 1.70 ม. จะซ้ำ 21.2% โดยคู่ที่แย่ที่สุดคือ
   แถบพื้นซ้ายเตียงกับขวาเตียง (ห่าง 3.40 ม. = 2 tile พอดี). วัดบนคู่ภาพจริงแล้ว
   **ไม่เจอ**: NCC ซ้าย-ขวา A -0.0170 (frac>0.5 = 0.224) → B -0.0287 (0.158) คือ
   **B ซ้ำน้อยกว่า A**; และ autocorr ของโปรไฟล์คอลัมน์ A +0.7548 @ lag 24 → B +0.7574 @
   lag 24 — **lag ไม่ขยับเลยทั้งที่เท็กซ์เจอร์เปลี่ยนขนาด 1.41 เท่า ซึ่งแปลว่ายอดนั้น
   ไม่ได้มาจากเท็กซ์เจอร์** (เป็นเรขาคณิต/แสง). สรุปที่ซื่อสัตย์คือ *เครื่องมือสองตัวไม่เจอ
   และตัวหนึ่งพิสูจน์แล้วว่ามองไม่เห็นสเกลเท็กซ์เจอร์* — ไม่ใช่ "ไม่มีการซ้ำ".
   ตาผมเห็นว่า B ดีกว่าชัด (แผ่นแคบลง รอยต่อถี่ขึ้น อ่านเป็นพื้นไม้จริง) — อยากให้ตาพี่ยืนยัน

**C2 cold critic (R7):** **ไม่ได้ยิงรอบนี้ และนี่คือหนี้ ไม่ใช่ข้อยกเว้น.** คู่ภาพรอบนี้เป็น
playblast (1200x900, sample ต่ำ) — bundle ของเลนนี้ทุกใบที่ผ่านมาเป็น full frame และการให้
กรรมการตัดสิน noise ของ playblast จะได้ข้อที่ไม่ใช่ของจริง. **owed: C2 + C3 บน full-fidelity
frame รอบหน้า** (`critic ladder lag` จะเริ่มนับจากรอบนี้)
**C3 (gate ใหญ่):** เหมือนกัน — ไม่ได้ยิง, owed รอบหน้า

**ข้อของกรรมการที่รอบนี้แตะ (จาก p2r64 ซึ่ง triage ไปแล้วในเกตของมันเอง):** ไม่มีข้อไหนถูก
"แก้" โดยรอบนี้. C2#8 (ลายไม้ผนังซ้ำ mirror axis) และ C2#13 (พรมเป็นสติกเกอร์) ยังยืนอยู่ครบ
— C2#8 อยู่กับ P2r-2 ซึ่งผ่าน cut ของตัวเองที่ lag 36 แต่ของจริงอยู่ที่ lag 364 ที่ crop
เอื้อมไม่ถึง (เกต p2r64 แถว 12 ACCEPT ไว้แล้ว); C2#13 ตอนนี้มีเลขวางข้างมันแล้ว (พรม 4.81x)
แต่ **ยังไม่มีใครพิสูจน์ว่าสองอย่างนี้เป็นเรื่องเดียวกัน**

**RANK (charter finish line):** NOT ASKED — นับเป็นหนี้ (ASK-006 ยัง 17 วัน)

**เข้า knowledge/ รอบนี้:** `knowledge/_inbox/web-engineered-oak-plank-widths-2026-08-24.md`
(REFERENCE tier, staged ยังไม่ distil). **เหตุผลที่ต้องมีไฟล์นี้คือความผิดของผมเอง**: หลักฐาน
ที่ผมหยิบมาตัดสินพื้นตอนแรกคือคอมเมนต์ `PLANK_PITCH_MM = 180.0  # 150-250 mm is the real range`
— เลขที่ **เราเลือกเอง** สำหรับ veneer ของเราเอง แล้วเอามาตัดสินภาพของเราเอง (รูป
self-consistency ที่ R7b เตือนไว้ตรง ๆ). ของจริงที่หามาได้: 189 มม. เป็นความกว้างที่มีขาย
จริงหลายเจ้า, extra-wide ขายที่ 220/260/300/385, และ ~267 มม. **หาคนขายไม่เจอ**

**Spend:** 2 playblast (ไม่มี full frame) · ~40 นาที wall-clock ของสองเรนเดอร์ (HIPEW ล้ม
→ Cycles ตกไป CPU ทั้งสองรอบ) · workflow วัดผล 7 agent / 934,841 token / 2,198 วินาที ·
**Reversible: yes** — `--floor-tile-legacy` คืนค่าเดิมได้ทุกเมื่อโดยไม่ต้องแก้ซอร์ส (และเลนนั้น
เขียนเป็น *ตัวคูณของขนาดที่ assert แล้ว* ไม่ใช่เลข 2.4 เปล่า ๆ จึงไม่มี literal ใหม่กลับเข้า tree)

**คิวที่ผมเสนอ (คำแนะนำ ไม่ใช่คำสั่ง):**
1. full-fidelity frame + C2 + C3 (หนี้ของรอบนี้)
2. พรม 4.81x — วัด pitch บนจอก่อน แล้วค่อยตัดสิน (แถว debt เขียน restart_by ไว้แล้ว)
3. P2r-2 ของจริงคือ repeat ที่ lag 364 ซึ่ง crop ปัจจุบันเอื้อมไม่ถึง — ต้องเป็น crop ใหม่
   ไม่ใช่การจูน shader รอบที่เจ็ด (R1: กลไกนี้ผ่านมาแล้ว 6 รอบ)

**ตัดสินรอบนี้:** ไม่มีแถวใหม่ใน `qa/open-decisions.json`. สิ่งที่ตัดสินลงทะเบียนใหม่แทน:
`qa/texture-scale.json` — TS-001 พื้น = ขนาดที่ประกาศ (undo: `--floor-tile-legacy`);
TS-002/TS-004 millwork ยืดตามเสี้ยน 1.5301x = **departure ที่เซ็นแล้ว** (undo: ลบ block
`departure`); TS-005 ผ้าทุกผืน 3.1399x = departure ที่เซ็นแล้ว โดยเหตุผลอยู่ในโค้ดมาก่อน
ทะเบียนนี้จะมี (undo: เหมือนกัน); TS-006 พรม 4.81x = **debt** มีวันที่และ restart_by
(undo: ลบแถว แล้ว ratchet จะแดง)

---

## สิ่งที่เครื่องจับคนเขียนได้เองในรอบนี้ (บันทึกไว้เพราะมันคือหลักฐานว่ามันทำงาน)

`texture_scale` ปฏิเสธผู้สร้างมันเอง **สองครั้ง** ก่อนจะเขียว:

1. กฎ 5 (`assert` grep โค้ดจริง) ตกเพราะหน้าต่าง regex ที่ผมเดาไว้ 1400 ตัวอักษรสั้นไป —
   วัดจริงแล้ว `grain_run_m` อยู่ห่างจากคีย์ 2547 และ preset ถัดไปเริ่มที่ 3015 จึงตั้งเป็น
   2900 (เลขที่วัด ไม่ใช่เลขที่เดา และมันกันไม่ให้ pattern เลื่อนไปแมตช์ preset ข้าง ๆ)
2. ตอนผมเพิ่มขา A/B `--floor-tile-legacy` บรรทัดที่ TS-001 assert ไว้เปลี่ยนรูป → rung
   แดงใส่ผมในนาทีเดียวกัน

และ **census ฉบับแรกของผมพลาดทั้งเลน**: sweep ที่มองหาแต่ `tile_m=` / `uv_scale=` /
`grain_run_m=` รายงาน 12 จุดและอ่านเหมือนสำมะโนที่สะอาด — TRN ทั้งสองเลนเก็บ tile เป็น
**สมาชิกตัวที่ห้าของ tuple** (`"veneer_oak": ((r,g,b), 0.60, 0.0, "wood_floor", 3.2)`)
ซึ่งไม่แมตช์คีย์เวิร์ดไหนเลย. **การ anchor กับคีย์เวิร์ดคือ allowlist ที่ต่ำลงไปอีกชั้น** —
มันระบุ *ไวยากรณ์* ที่ตัวเองคุม แล้วยกเว้นไวยากรณ์ถัดไป. เปลี่ยนไป anchor กับ **slug**
(ซึ่งเป็นข้อมูลที่เรามีอยู่แล้ว) สำมะโนขึ้นจาก 12 → 30

## เลขที่ผมเขียนผิดแล้วแก้ในรอบเดียวกัน

- "แผ่นพื้น 254 มม. / ของจริง 180 มม." — **ผิดทั้งคู่**, ได้มาจากการถอยเลขจาก
  `PLANK_PITCH_MM` ของเราเอง. วัดจากแผนที่จริง: 9 รอยต่อต่อ tile (ยืนยันด้วย FFT k=9 และ
  autocorrelation), pitch **188.889 มม.**, เรนเดอร์ที่ 2.4 ม. = **266.67 มม.**
  ทิศทางของข้อสรุปแข็งขึ้น ไม่ใช่อ่อนลง
- "ผนัง 2.2 ม. = 1.2941x" — **เลขถูก แต่ไม่มีพิกเซลไหนใส่มัน**: ทั้งสองจุดอยู่ใต้
  `if n.startswith("wall_0")` และฉากที่สร้างจริงมีวัตถุชื่อขึ้นต้น `wall_0` **ศูนย์ตัว**
  (มี 33 ตัว: wall_1z, wall_2, wall_3*, ...), จุดที่สองยังถูก gate ด้วย `_wall_pbr` ซึ่งเป็น
  False, และวัสดุที่ branch นั้นจะใส่มาจาก `feature_wall = "oak_veneer"` = factory
  `proc_wood` ที่ไม่อ่าน UV เลย. **เลขคณิตที่ถูกต้องบนการแมปที่ไม่มีใครใส่ = คำประกาศ
  เกี่ยวกับรูป ไม่ใช่รูป** ซึ่งคือข้อบกพร่องที่ R11 เขียนมาปิดพอดี — และฉบับแรกของทะเบียนนี้
  ทำมันเอง. บันทึกไว้ใน `_dead_sites` และ **ไม่แก้** (แก้ dead code แล้วรายงานว่าเป็นการ
  เปลี่ยนภาพ คือความผิดอีกแบบหนึ่ง)
- สำมะโนพิกเซล 5.11% / 44.32% / 10.08% ที่แผนอ้างว่าเป็นปัจจุบัน — เป็นของ **p2r57**
  เจ็ดรอบก่อน และ 44.32 ไม่ reproduce แม้แต่บน p2r57 (ได้ 55.40). วัดใหม่บน p2r64 ด้วย
  `value_probe.decode_ids`: พื้น **4.42%** · millwork oak **26.86%** (ทั้งตระกูล 37.57%) ·
  พรม **4.23%**. ข้อสรุปของแถวนั้นแข็งขึ้น: พื้นไม่ใช่ผิวใหญ่สุด และตอนนี้ **เล็กกว่า**
  5.11% ที่มันเถียงไว้ตอนแรกด้วยซ้ำ
- และเลข autocorr ของ P2r-2 ที่ `plan_status` พิมพ์ทุก session (0.597 vs 0.564) —
  **ค้างมา 15 รอบ**. เป็นคำพูดที่ freeze ไว้ในช่อง `goal` ตั้งแต่ review #57 แล้วถูกพิมพ์ซ้ำ
  เหมือนเป็นการวัดสด. ของจริงบน p2r64: **0.477 @ lag 36 vs floor 0.509 = ไม่เป็นคาบ ผ่าน**
  และผ่านมาตั้งแต่ p2r55. **การวัดที่ถูกอ้างเป็นร้อยแก้ว ไม่มีใครรีเฟรชให้ได้**
