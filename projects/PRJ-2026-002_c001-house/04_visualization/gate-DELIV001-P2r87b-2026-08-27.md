# GATE — DELIV-001 P2r87b · 2026-08-27 · ราวแขวนกลมแล้ว + สาเหตุที่วัดได้ของ "แขวนอยู่กับอะไรไม่รู้"

**คำสั่งที่รอบนี้รับใช้:** P2r-28 (millwork cannot-be-built) · ORD-2026-08-05-object-must-justify-itself
· ORD-2026-08-27-builder-calls-its-own-skills (**รอบนี้ทำให้มัน obeyed**)

**ภาพ:** `pipeline/output/room_bedroom_suite_eye_p2r87_ql.png` (R5 playblast, 48 spp, 1200×900)
**ไม่มี full frame รอบนี้ และตั้งใจ** — R1 CAP เหลือ **2 รอบ / 14 เฟรม** ก่อนหยุดบังคับ
และรอบนี้เปลี่ยน "รูปทรงของชิ้นเดียว" ซึ่ง playblast ตัดสินได้ (R5)

**crops:** [p2-exit-crops/bedroom_suite_eye_p2r87/](p2-exit-crops/bedroom_suite_eye_p2r87/)

---

## 1. รอบนี้เริ่มด้วยการวัด ไม่ใช่การแก้

`id_mask` บนเฟรมเต็มล่าสุด (p2r85, 2400×1800) — **ราว 9 เส้น มีแค่เส้นเดียวที่กล้องเห็น**:

| object | px | ขนาดที่ปรากฏ |
|---|---|---|
| `rail_full` | 3618 | bbox 154×81 · **หนา 23.5 px** · เริ่มที่ **x=0 (ตกขอบเฟรม)** |
| `rail_full_sock1` (ที่ยึดขวา) | 433 | 30×28 px |
| `rail_full_sock0` (ที่ยึดซ้าย) | **0** | **อยู่นอกเฟรม** |
| ราวอีก 8 เส้น + socket 15 ตัว | **0** | อยู่นอกเฟรมทั้งหมด |

**สิ่งที่การวัดเปลี่ยน:** ข้อของ C2 ("ที่ยึดอ่านไม่ออก") ไม่ใช่เรื่องของราวทั้งเก้า แต่เป็นเรื่องของ
**ที่ยึดหนึ่งตัวขนาด 30 px** — และปลายอีกข้างของราว *ถูกครอบตัดออกไป* ไม่ใช่ไม่มี

## 2. เป็นปัญหาของกล้องหรือของวัตถุ — ถามเครื่องมือของกล้อง

เรียก skill `camera-composition` (ครั้งแรกที่ roster ถูกเรียกตามคำสั่ง 08-27) · `frame_geometry`
ตอบทั้งสามข้อ **ผ่านหมด**: ยอดผนัง BF14 v=0.3454 < 0.375 (อยู่ในเฟรม) · จุดสัมผัสพื้นของม้านั่ง
อยู่ในเฟรม · DoF เอื้อมถึงและมวลใกล้สุดคม

→ **ไม่ใช่ข้อบกพร่องของกล้อง** และกล้องคือ D-152 ของพี่ที่ล็อกไว้แล้ว · skill ข้อห้ามของมันเองบอกว่า
ห้ามเปลี่ยนกล้องกับวัตถุในรอบเดียวกัน → **งานทั้งหมดของรอบนี้อยู่ฝั่งวัตถุ ที่ปลายซึ่งอยู่ในเฟรม**

## 3. สิ่งที่แก้: รูปทรงถูก derive จากชิ้นส่วน ไม่ใช่จากชื่อ (D-162)

`RAIL_D` แบกคำว่า *"round rail modelled square"* มาตั้งแต่วันแรก และ C2 อ่านออกจากเฟรมสองครั้ง
ที่ 24 px หน้าเหลี่ยมกับ gradient ของทรงกลมไม่ใช่ภาพเดียวกันแน่นอน

- `millwork.rod_axis(dx,dy,dz)` — pure: หน้าตัดจัตุรัส (ต่างกัน ≤2มม.) **และ** ยาว ≥4 เท่าหน้าตัด = rod
- `build_room.add_rod` — ท่อ smooth-shaded ที่ **inscribe อยู่ใน AABB เดิม** → bbox, placement,
  load path, anchor registry อ่านเหมือนเดิมทุกตัว เปลี่ยนแค่ silhouette กับ specular
- **scope มาจากกฎเอง ไม่ใช่รายชื่อ (R9b):** sweep 9 kind × 2 axis → คัดได้ 21 ชิ้น **ทุกชิ้นคือราวแขวน**
  ไม่มี panel/shelf/batten/cleat และไม่รวม socket ของราวเอง · ตัวที่ถูกปฏิเสธใกล้ที่สุดอยู่ต่ำกว่าเกณฑ์ 13 เท่า
  ตัวที่ถูกรับใกล้ที่สุดสูงกว่า 6 เท่า → เลขไม่แบกน้ำหนักที่ทศนิยมที่สาม

**LOOK ยืนยัน** (crop `rail-square-vs-round.png`, ซ้าย p2r85 เหลี่ยม / ขวา p2r87 กลม): ท่อทองเหลือง
ได้ gradient ที่บอกว่า "กลม" แล้ว · dump ยืนยัน: polys 216, smooth 216, curved_clusters 11

## 4. สิ่งที่ LOOK เจอและใหญ่กว่าสิ่งที่ตั้งใจมาแก้

crop `rail-cell-and-garment.png`: เสื้อในช่องที่กล้องเห็น **ไม่ต่อกับราวในภาพ** — และ probe บน
.blend ตอบว่าทำไม โดยไม่ต้องเดา:

```
mill__style_garmentacq8s0_hanger   type=CURVE  top=1.8974  bevel_depth=0.0018
rail_full                          z 1.8500-1.8800
```

**ไม้แขวนมีจริง เกี่ยวราวถูกต้อง (ยอด 1.8974 > ราว 1.8800) และเรนเดอร์อยู่** — แต่มันคือลวด
**Ø3.6 มม.** ขณะที่ราว 30 มม. อ่านได้ 24 px → **ไม้แขวนอ่านได้ ~3 px ในเฟรมส่งจริง**
คือเส้นผม · นั่นคือคำตอบที่วัดได้ของข้อ C2 *"เสื้อแขวนอยู่กับอะไรไม่รู้"*

**และทางแก้ไม่ใช่ทำให้มันหนาขึ้น** — millwork เขียนกฎของตัวเองไว้ว่า *"We never fatten a dimension
to make it visible"* · ของจริงที่อ่านได้ในภาพงานส่งคือ **ไม้แขวนไม้ หนา 10-12 มม. (~8-10 px)**
ฉะนั้นนี่เป็นคำถามเรื่อง **คลาสของ asset** (R8 ACQUIRE) ไม่ใช่การขยายตัวเลข → **รอบของมันเอง**

**ทำไมไม่มีเครื่องไหนเห็น:** ไม้แขวนเป็น CURVE และ scene dump เก็บแต่ MESH — `carry_check`,
`placement_check`, D7 ไม่มีตัวไหนมองเห็นมันเลย ทั้งที่มันคือชิ้นที่ตอบคำถาม "อะไรรับมวลนี้ไว้"

## 5. คำสั่ง 08-27 ถูกทำตามโดยกลไกของมันเอง — และเกตเป็นคนบังคับ

รอบนี้ยิง build ครั้งแรกแล้ว **RULE GATE FAILED ด้วย violation เดียว**:

> `ORD-2026-08-27-builder-calls-its-own-skills is filed NOT-OBEYED while every assertion it carries holds.`

เพราะการเรียก `camera-composition` ในข้อ 2 ทำให้ hook เขียนแถวลง `qa/skill-usage.jsonl`
และ `obeyed_assert` ข้อแรกของคำสั่งก็ match ขึ้นมาเอง · **ไม่มีใครไปแก้แถวให้มันเป็นจริง —
พฤติกรรมเป็นคนทำ และเกตเป็นคนยืนยันโดยไม่ยอมเรนเดอร์จนกว่าบันทึกจะซื่อสัตย์**

## 6. รั้วอื่นในรอบนี้

`CARRY: 567 มวล · 143 ยืนบนพื้น · 32 ไม่มีคนรับ (19 ในเฟรม) ทั้ง 32 มีแถวใน ledger` → OK ·
`orders_check` 0 · `decisions_check` 0 · **327 + 213 tests OK** ·
`SCORE DELIVERABLE: DOES NOT QUALIFY — D1 NOT RUN` ซึ่งถูกต้องสำหรับ playblast (D1 วัดที่ res ส่งจริง)

## 7. รอบนี้ **ไม่ปิด** P2r-28

ปิดได้แค่ครึ่ง: ข้อ "แท่งเหลี่ยมแบน" ตายด้วยการวัด + การมอง · เงื่อนไขปิดที่เหลือคือ
**เฟรมเต็มหนึ่งใบที่กรรมการทั้งสองไม่ยื่นข้อราวลอย** ซึ่งตอนนี้มีสาเหตุที่ตั้งชื่อได้แล้ว (ไม้แขวน 3 px)
· ladder ยังไม่ได้ยิงรอบนี้ **โดยตั้งใจ**: C2/C3 ตัดสินเฟรมเต็ม ไม่ใช่ playblast 48 spp
และ R1 CAP เหลือ 2 รอบ — เฟรมเต็มใบต่อไปต้องคุ้มค่าที่สุด

**เสนอเป็นรอบถัดไป (P2r-34):** คลาสไม้แขวน — ไม้แขวนไม้ ~10-12มม. แทนลวด 3.6มม.
(fetch ตามโปรโตคอล free-baseline-first ที่เพิ่งกลายเป็น guard) แล้วยิงเฟรมเต็ม + ladder ครบทั้ง C2/C3
ในใบเดียว

## 8. กลับทาง

`git revert <sha>` · หรือลบ 4 บรรทัด `_ax = millwork.rod_axis(...)` ใน materializer แล้วทุกชิ้น
กลับเป็นกล่อง · ลบ D-162
