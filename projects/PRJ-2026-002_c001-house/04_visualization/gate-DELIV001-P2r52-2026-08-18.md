# gate-DELIV001-P2r52 — 2026-08-18 — the whole bed lands: integration round of the audition winner

**IMAGE PAIR** (the round's record, R2):
`pipeline/output/room_bedroom_suite_eye_p2r49.png` (bare mattress flank 254,849 px —
'most', 3/3 blind readers) vs
`pipeline/output/room_bedroom_suite_eye_p2r52.png` (the same room, f52472c1 staged by
the build's own frame hook — bare flank 1,459 px, 'none' 3/3 blind readers).

**THREE LINES**
1. The D-106 winner is IN THE BUILD, not the bench: `bed_models.frame` in the spec
   drives `_place_bed_frame`, which stages the bought bed the way the audition did
   (unit factor asserted, head end measured, strip geometric) plus the five
   enumerated integration debts — and the frame QUALIFIES on deliverable_check
   for the first time in the lane (6/6 mandatory rows).
2. The defect this phase re-aimed around is measured off the frame: core_flank
   254,849 -> **1,459 px** (99.4% fall, ratchet lowered), and the blind panel that
   read every prior frame 'most' 3/3 reads this one **none 3/3** with the p2r49
   control still 'most' 3/3 — the instrument held in both directions.
3. Neither critic filed a bare-mattress item — the first time BOTH lanes are silent
   on the class in this lane's history. Both critics' #1 is now the same SURFACE
   item (the duvet's fold sharpness / sheen), which is exactly the phase's next
   named work (P2h / P2r-5 / P2r-22).

## The gate the round was declared against (P2r-21)

> bed_pixels core_flank strictly below the 254,849 px baseline AND no blind reader
> above sliver on the full-fidelity frame; exit 2 anywhere = the round did not run.

| clause | result |
|---|---|
| core_flank < 254,849 | **1,459 px** (core_top 608; core_px 2,067 of a 355,844 px sleeping surface = 0.6%) |
| blind readers ≤ sliver | **none / none / none** (A/B/C, shuffled, unlabelled) |
| control p2r49 | most / most / most — the instrument still catches the defect it was built for |
| exit 2 anywhere | none — bed_pixels ran (roles resolved), p2_exit's two absent-cloth rungs print N/A BY DECLARATION (D-083/D-107), not could-not-run |

**PASS.** Baseline lowered to the new numbers (`qa/bed-pixels-baseline.json`, may only
ever go down). The p2r31e hand-built record (11,694 px) is beaten 8x by the bought
bed — recorded in the baseline's best_on_record note so nobody reads the old record
as an instruction to revert (D-104 stands).

## What was built (the D-107 mechanics, tests green)

- `wholebed_rules.py` + tests: three INTEGRATION strip rules, geometric, frozen
  after an id-coloured render of the file settled the part identities the p2r51
  gate had inferred (and partly mis-inferred: the "two burl wing panels" are the
  two visible ends of ONE full-width panel; the below-plane pair are shelf boards;
  the plaid is a lying-flat lumbar band). `frame_cluster` = the scale denominator.
- `build_room.py::_place_bed_frame` (~300 lines): sidecar door (`whole_bed` band
  added to asset_scale, sidecar asserted 3159 mm maxxy), bench-order staging,
  strip, fit on the frame cluster (fill re-checked on the WHOLE kept bed — the
  cluster fills 0.80 under bedding that drapes to 0.90, and that is the winner's
  construction, not a different bed), roles for bed_pixels (six studio tokens; the
  print killed by full material replacement — 3DW ships base-colour-only so DR §2's
  keep-normal/rough clause is vacuous, the crumple lives in 225k tris of geometry),
  ladder register rows (`ACQUIRED_AS["bed__base"]`, `DECLARED_ABSENT["bed__coverlet"]`),
  and the head-end bare strip measured by 64 ray rows and closed with
  `bed__head_shim` — a 265 mm modelled upholstered pillowstop (DR whole-bed-swap §1),
  named `head_shim` because D8 correctly reads "pillow" as an acquire-class word
  and this board is correctly BUILT joinery.
- Three build bugs caught by the quick rung before any full frame was spent (R5):
  fill measured on the wrong denominator; `_bail` touching deleted objects;
  the mattress classified as "tops out at the plane" when the ray plane is a
  MEDIAN mixing bare mattress (347) with duvet loft (519) and is not any part's
  face — the mattress is the layer BETWEEN frame back and plane.
- 16-row shim sampling measured the 245 mm bare strip as 0 (134 mm row spacing
  straddled a ~100 mm strip while the id-mask counted 921 px in the same frame) —
  64 rows found it. The miss is in the code comment.

## The blind flank panel (re-run, same construction)

`_private/deliv-001/flank-panel-p2r52-2026-08-18/` — QUESTION.md byte-identical,
9 frames (7 delivered + p2r52 + p2r49 control), 1600 px, per-reader shuffle,
`readings.json` decoded against held-out keys after all three returned.

| frame | A | B | C |
|---|---|---|---|
| **ours p2r52** | none | none | none |
| ours p2r49 (control) | most | most | most |
| delivered ×7 | 17 legible readings none, 1 partial (A saw a real bare stretch on one delivered right flank — recorded as-is) |

Reader A on ours, unprompted: *"at the right head-corner a small white glimpse is
the sheet at the top surface, not the side face"* — the residual 608 top px read
blind as a made bed's sheet, not as the defect class.

## Critic ladder (R7b/R7c), full triage

C2 = fresh-context blind local (`ANSWER_claude-local-c2.md`), C3 = Gemini 2.5 Pro
live API (`ANSWER_gemini25pro.md`), both archived in
`_private/deliv-001/critique/critique-room_bedroom_suite_eye_p2r52/` before triage.

| # | item (source) | triage |
|---|---|---|
| 1 | duvet folds angular/foil, sheen plastic; "no base line, cloth reads as a pile on the rug" (C2#1, C3#1) | **ACCEPT (surface half) → P2h/P2r-5/P2r-22** — the standing surface lever, now on the acquired cloth. The no-base-line half is PART-REFUTED by measurement: `bed__frame__acq0` renders 37,976 px and blind reader C named "the darker gray box base beneath the hem"; the delivered pool itself puddles to the floor in 4 of 7 frames by the readers' own words. The foot face being fully draped is the winner's construction, panel-accepted. |
| 2 | drawer tower tilted, floating, raw concrete (C2#2) | **REFUTED WITH A MEASUREMENT** — the three fronts share identical world x/y extents (4.1071–4.5686 / 7.9958–8.0158) and stack square at 230 mm pitch; a tilt cannot produce equal AABBs. Floating + handleless + microcement are the signed D6-A identity. Tally kept: 2nd critic to read the microcement as raw site concrete → noted on the P2r-5 material-response row. |
| 3 | Louis nightstand style clash, empty carcass, top far from bed surface (C2#3) | ACCEPT → standing room-debt row (first filed p2r51 C2#7). The HEIGHT half opens a real seam: nightstand top 618 vs the acquired plane 425 = +193 mm, outside the P2i band — and the spec's bed h=600 no longer describes the built bed. → **NEW ROW P2r-24**. |
| 4 | garments = shiny plastic bags; rail heights unequal; hem touches shelf (C2#4, C3#3) | ACCEPT → P2r-20 (R1-stopped, enters as its own mechanism per the plan) |
| 5 | sconces cast no scallops; wall pools have no visible source; lamp off / shade "glows" (C2#5, C3#2) | ACCEPT → P3 (light phase, deliberately last per the owner's build order); the lamp-shade emission physics also noted on P2r-23's fixture-internals row |
| 6 | ottoman vinyl/inflated (C2#6) | ACCEPT → P2h + ASK-024 (standing) |
| 7 | pelmet ends raw mid-air at the wardrobe junction; switch plate floats across slat battens (C2#7) | ACCEPT → appended to **P2r-23** (the construction half it already owns: end-cap the pelmet, mounting block under the plate) |
| 8 | bed off-axis between the sconce pair (C2#8) | **REFUTED WITH A MEASUREMENT** — frame y-centre 1.126 m vs sconce midpoint 1.125 m; gaps 0.850 / 0.850 m equal to the millimetre. The read is perspective (C2 hedged it itself at r51). The empty two-tone side box → P4 styling; the curved object clipping the right edge is the second ClassicNightstand — covered by the same style-clash row as #3. |
| 9 | staging density low, monochrome, no art/curtain traces (C2#9, C3#6) | ACCEPT → P4 (the phase that exists for exactly this; enters after P2 per the 08-16 order) |
| 10 | wood grain tiling + razor arrises on millwork (C3#4) | ACCEPT → P2r-2 (the instrument agrees: autocorr peak 0.585 @ lag 199 px vs floor 0.57, PERIODIC — same lag nine rounds running) + arris chamfer noted on P2r-22's construction-cues row |
| 11 | overall light flat (C3#5) | ACCEPT → P3 |
| 12 | left-edge vertical sliver unresolved (C2#10) | NOTED — the mirror-bay edge at the camera crop; carried as a framing note, not an object defect |

**Phase-exit reading (not this round's gate):** both critics' top-3 still carry one
surface-realism item each (the duvet surface) → P2's reopened bar clause (3) is not
met; the phase continues on P2h → P2r-2 → P2r-22 → P2r-23 exactly as queued.
C3 raw item count for the reporting line: 6 (was 6 at p2r51, 7 at p3r1).

## What is DECIDED

- **D-107** (filed this round, `qa/open-decisions.json`): the integration mechanics —
  geometric strip rules + frame-cluster fit + full material replacement by role +
  measured head shim + per-staging orientation. Reverse: delete `bed_models.frame`
  from the spec (the cloth path reads its kept rows again), or a failed second
  round moves to the D-106 alternates, never the same candidate a third time.
- The ratchet baseline is LOWERED to p2r52's numbers — the one direction it can move.

## Spend (R6)

1 identification render (workbench, 3 views) · 5 Blender builds (3 quick — two of
which caught real bugs before any full spend — + 2 full) · 2 full-fidelity frames ·
1 Gemini pro call (usage bumped) · 4 fresh-context agents (C2 + 3 panel readers) ·
3 exploration agents at session open · 1 NLM ask (P2i, NOT-IN-SOURCES, staged) ·
tests: 28 wholebed + 66 ladder + 26 asset_scale + 58 bed_pixels + 23 p2_exit +
113 placement/rationale green.

Nothing waits on his signature (R3); he overrules from the image at any time.
