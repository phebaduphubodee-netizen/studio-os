# DELIV-001 — P2 round 1 (surface) — 2026-08-10 — gate #2

**ภาพ:** ก่อน `pipeline/output/room_bedroom_suite_eye_p1a.png` |
หลัง `pipeline/output/room_bedroom_suite_eye_p2a.png` (ทั้งคู่ 2400x1800 full-fidelity)

1. **แก้อะไร:** เปิด `_FABRIC_MAPS` เป็นค่าตั้งต้น (D-022) — โค้ดแผนที่ผ้าถูกเขียนไว้บน
   วัสดุ soft goods 7 ตัวตั้งแต่ 2026-07-30 หลัง flag ที่ไม่มีเลนไหนเคยเปิด ·
   object ที่มี image map: **2/498 → 13/498**
2. **ผมตัดสินว่า:** PASS แบบจำกัด — **เครื่องมือตัดสินไม่ได้ ตาตัดสินได้** (octave energy
   10.1222 → 10.1223 = ไม่ขยับ; crop 2x ที่หัวเตียงเห็นชัดว่า euro sham อ่านเป็นผ้าทอ)
   ทางที่ทิ้ง: ไม่แตะ `oak_veneer` ในรอบเดียวกัน เพราะมันเป็น preset ที่ owner เซ็น
   และสีถูกเลือกหลัง LOOK — ต้องมี A/B และคำตัดสินของตัวเอง
3. **ไม่แน่ใจ:** **C3 ไม่ได้เปลี่ยนคำตัดสินเรื่องหมอนเลย** หลังเปิดแผนที่ผ้า — ยังบอกว่า
   "หมอนดูแข็งเหมือนก้อนหินหรือพลาสติกที่เติมลม" เหมือนเดิม แปลว่าครึ่งพื้นผิวไม่พอ
   และครึ่งที่เหลือคือเรขาคณิต (ไม่มีตะเข็บ กลมเกินไป ไม่มีเงาสัมผัส) — **อยากให้พี่ดูว่า
   ควรทุ่มไปทางหาโมเดลมาวาง หรือทางเย็บตะเข็บใส่ของที่มีอยู่**

**C2 cold critic (R7c): NOT FIRED — หนี้ต่อเนื่องจาก gate #1.** เหตุผลเดิม (session นี้มี
คำสั่งห้ามเรียก Agent tool เว้นแต่เจ้าของสั่ง) · ask ที่ blind แล้วรออยู่ที่
`_private/deliv-001/critique/critique-room_bedroom_suite_eye_p2a/c2-ask/`
**สองรอบติดที่เดินด้วย C3 ขั้นเดียว — นี่คือราคาที่ R3 บอกไว้ว่าต้องจ่าย**

**C3 (Gemini 2.5 Pro, R7b):** ยิงแล้วบนเฟรม p2a คำตอบอยู่ที่
`.../critique-room_bedroom_suite_eye_p2a/ANSWER_gemini25pro.md` — 5 ข้อ triage ครบ:

| id | ข้อกล่าวหา | triage |
|----|-----------|--------|
| C3#1 | เสื้อผ้าในตู้แข็งเหมือนกระดาษ ไม่ทิ้งตัว (ร้ายแรงสุด **ซ้ำจากรอบก่อน**) | **✓ รับ → P2a (blocked, procurement).** D8 ชี้ garment 6 ชิ้นเป็น box-like ตรงกัน และ build log มี `ANALYTIC fallback` 9 ครั้ง · **แต่ชั้นวางของเราไม่มีอะไรมาแทน**: garments_set_a เป็นราวยาว 4 ม. (out-of-band), shirt_hanger_a หนา 12 มม. = planar ซึ่งคือ defect เดียวกันในรูป asset |
| C3#2 | หมอนแข็งเหมือนก้อนหิน ใบหน้าสุดลอย ไม่มีเงาสัมผัส (**ซ้ำ และไม่ขยับหลังเปิดแผนที่ผ้า**) | **✓ รับ → P2c/P2d.** วัดยืนยัน: n90 52-54, smooth เต็ม → ไม่ใช่ปัญหา shading และไม่ใช่ปัญหาวัสดุอีกต่อไป **เป็นเรขาคณิตล้วน** · การที่คำตัดสินไม่ขยับหลัง D-022 คือหลักฐานว่าครึ่งพื้นผิวไม่พอ ไม่ใช่หลักฐานว่า D-022 ผิด |
| C3#3 | **ไม่มี contact shadow** — ขาม้านั่งไม่จมพรม ฐานเตียงลอย | **✓ รับ → DEBT-02 ซึ่งเป็นแถวที่ "ยังไม่มีเครื่องมือมองเห็น"** · เขาชี้ตรงเป๊ะกับแถวหนี้ที่เราเปิดค้างไว้เพราะไม่มี probe · **นี่คือ R7b ในหนึ่งบรรทัด: ตาเจอสิ่งที่เครื่องมือของเราถูกออกแบบมาให้มองไม่เห็น** |
| C3#4 | ไม้ตู้ไม่มีลายไม้/ไม่มี bump/ไม่มี specular (**ซ้ำจากรอบก่อน**) | **✓ รับ → P2g (เปิดใหม่รอบนี้ พร้อมตัวเลข).** `m_millwork_oak_veneer` อยู่บน **163 จาก 498 object** และ factory เป็น `proc_wood` (ลายไม้ procedural สองสี ไม่มีรูปถ่าย) · กลไก `_pbr_material(..., base_tint=)` มีอยู่แล้วและรักษาสีที่เซ็นไว้ได้ · แผนที่อยู่บนดิสก์แล้ว 9 ชุด |
| C3#5 | หนังสือบนม้านั่งเป็นบล็อกขอบคม วางซ้อนสมบูรณ์แบบเกินจริง | **✓ รับ → P4 (styling).** R8 บอกว่าหนังสือเป็น free-form → ACQUIRE · ชั้นวางมี **หนังสือ 0 เล่ม** (CATALOG shortfall: usable_styling 4, short_by 8) → หนี้จัดซื้อเช่นกัน |

**ไม่มีข้อไหนถูกหักล้าง** · **3 ใน 5 ซ้ำจากรอบก่อน** ซึ่งเป็นข้อมูล ไม่ใช่ความล้มเหลว:
รอบนี้แก้พื้นผิวของ soft goods อย่างเดียว และเขาชี้ว่าไม่พอ ซึ่งตรงกับที่ D-022 เขียนไว้เอง

**RANK (charter finish line):** **NOT ASKED — หนี้ต่อ** (เหตุผลเดิม: P5 เป็นเจ้าของ RANK
และเลนยังอยู่ P2 · ถามตอนนี้คือใช้สิทธิ์ถามไปกับเฟรมที่ P2 ยังทำไม่จบ)

**เข้า knowledge/ รอบนี้:** ยังไม่มีไฟล์ · **บทเรียนที่โตพอจะเข้าแล้วและควรเป็นงานรอบหน้า:**
"**0 จาก reader ที่มองไม่เห็น ไม่ใช่ 0**" (พบ 2 ครั้งวันนี้: scene rows กับ catalog slot) และ
"**ทุกขอบเขตที่รับ default encoding คือความล้มเหลวที่แสดงตัวเป็น 'ไม่เจออะไร'**" (พบ 3 ครั้งวันนี้)

**Spend:** 8 builds (2 full + 6 quick) · 8 renders · full render 8m31s ×2 ·
lane wall-clock รอบนี้ ~5h · Gemini 2 calls (pro) ·
**Reversible:** yes — `--no-fabric-maps` ดูขา A ได้โดยไม่แก้ source; D-022 ระบุวิธี revert

**คิวที่ผมเสนอ (คำแนะนำ ไม่ใช่คำสั่ง):**
1. **P2g — แผนที่รูปถ่ายบนไม้โอ๊ค 163 object** C3 ชี้ 2 รอบติด, ตัวเลขใหญ่สุด, กลไกพร้อม
2. **DEBT-02 contact shadow** C3#3 ชี้เอง และเป็นแถวที่เรายอมรับว่ายังไม่มีเครื่องมือ
3. **P2a procurement** — ต้องการคำตัดสินเรื่องเงินจากพี่ (ดูรายงาน session)
4. **P2c เย็บตะเข็บ/ซับดิไวด์หมอน** ถ้าไม่ซื้อ ก็ต้องแก้เรขาคณิตของที่มีอยู่

**ตัดสินรอบนี้:** **D-022** (`_FABRIC_MAPS` เปิดเป็นค่าตั้งต้น) — undo: ตั้งกลับเป็น False
ที่ `pipeline/scripts/build_room.py` แล้วลบแถว D-022 · รายละเอียดใน `qa/open-decisions.json`
