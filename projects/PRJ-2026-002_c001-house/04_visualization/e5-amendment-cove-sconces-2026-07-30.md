# Element 5 amendment — cove + paired sconces (2026-07-30)

## Authorization trail (R3: only the owner closes)
The ask stood, unchanged, at three consecutive gates:
- gate #6 (lane A close): "(a) อนุมัติแก้ e5: เพิ่ม cove + sconces" — owner reply was the
  round-6 continuation.
- gate #7 (A+C): question (ก), second unanimous critic vote.
- gate #8 (lane B): question (2), THIRD unanimous vote — bench sheet, owner's own
  Gemini walk, C2 on lc1, C2 on lb1 all name light-origins as a top sellability gap.
- Owner's reply to gate #8: **"รออะไร?"** — recorded here as the approval to proceed
  on the standing recommendation. If this reading is wrong, the amendment is one
  commit and reverts clean; the CD state without the story flag renders the
  amended plan too (a fixture is a fixture, not a photoshoot state).

## What was added (all positions DERIVE from signed geometry)
- **Cove**: warm LED strip behind a 20mm pelmet fascia along the FULL BF14 run,
  z = ceiling − 150mm, strip 90mm off the slat face, grazing DOWN the wall.
  Watts = 10 W/m × run. The fascia is the visible answer to "แสงมาจากไหน".
- **Sconces ×2**: brass cylinders (Ø60×140mm, 45mm off the slat face) flanking the
  bed centreline at ±850mm, z 1550mm, up/down 6W; story mode drives them with the
  BEGA 6339 surface-wall IES profile (1.IES from the fetched LM-63 pack).
- Schedule rows emitted with the plan (documentation law — the frame and the
  fixture schedule must tell one room); derivation + schedule pinned in
  `test_element5_lighting.py` (off-run sconce RAISES, never clamps).

## What was NOT done, deliberately
- **Headboard panel** (C2 ×2): collides with the signed "the headboard IS wall
  BF14" + the flush bed head. The cove's graze + sconce pools are expected to
  resolve the "floating pads" read; if the next critic still calls it, the panel
  question returns to the owner AS a question.
- **Skirting / bulkhead / door** (architecture finish): our own CD elevations
  (cd-set_v01) carry NO skirting layer — the ink is silent, so building it in the
  render would draw architecture nobody decided. Standing owner question.
