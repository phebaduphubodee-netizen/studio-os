# GATE — DELIV-001 P2r87 · 2026-08-27 · รอบเครื่องมือ (ไม่มีเฟรม และตั้งใจ)

**คำสั่งที่รอบนี้รับใช้:** ORD-2026-08-27-builder-calls-its-own-skills (ใหม่, **not-obeyed**)
· ORD-2026-08-22-blenderkit-one-month (ป้องกันความเสียหายซ้ำ) · ORD-2026-08-04-stop-and-ask-for-critique

**ไม่มี render รอบนี้:** ไม่มีพิกเซลเปลี่ยน และไม่มีข้ออ้างว่ามี. รอบนี้แก้ชั้นที่ p2r86
พิสูจน์ว่าพัง — ชั้นที่ตัดสินใจว่า *จะทำอะไรก่อน* ไม่ใช่ชั้นที่วาดรูป

---

## 1. รอบนี้เริ่มจากคำถามของพี่ ไม่ใช่จากคิวงาน

พี่ถามว่า *"ดีที่สุดแล้วหรือยัง สิ่งที่เสนอมา"* หลังผมเสนอลิสท์ skill — และการตรวจตัวเองเจอ
**ห้าข้อผิด สามข้อพลิกคำตอบ**:

1. **เลือกกลไกผิดชั้น.** เสนอ `acquire` เป็น skill ทั้งที่ `blenderkit.py fetch` ไม่เคย
   ปรึกษาโปรโตคอล baseline เลย — เช็คนั้นมีชีวิตอยู่ในที่เดียวคือ `print` ใน `status`
   R11 เขียนรูปนี้ไว้เอง: *"declared mandatory and then printed as a suggestion"*
2. **ข้อเท็จจริงผิดที่พลิกข้อสรุป.** skill ทั้งหกไม่ได้ "ไม่เคยถูกใช้" — มันผลิต `concept.md`
   (07-02), `ffe-candidates.json` (07-04), `bom.md` (07-02) แล้วหยุด. **มันไม่ได้ตายเพราะถูก
   ละเลย มันตายเพราะงานเปลี่ยนรูป** → ชั้น stage skill คือ *หลับถูกแล้ว* ไม่ใช่ *ขาด*
3. **ลืมสองอย่างที่แรงกว่าครึ่งลิสท์**: agent definition ของ C2 (ตาบอดถูกพิมพ์มือใหม่ทุกรอบ
   และเคยรั่วมาแล้วหนึ่งครั้ง) และ `camera-composition` ทั้งที่ D-152 คือหลักฐานที่แรงที่สุด
   ในเลน (กล้องของพี่ทำให้ clause แรกผ่านครั้งแรก 6/4 → 8/2 โดยของไม่เปลี่ยนสักชิ้น)
4. **เกณฑ์รับของไม่มีเครื่องวัด** — "ภายใน 3 รอบต้องมีหลักฐานว่าถูกเรียก" โดยไม่มีอะไรใน
   รีโปนับ invocation ได้เลย = R13 *"obedience there is only declared"* ยกขึ้นอีกชั้น
5. **ไม่ได้บอกว่ามันแลกกับอะไร** — ไม่มีข้อไหนอยู่บน critical path ของ P2

พี่ตอบ **"เอาหมด ลุยยาว ๆ"** แล้วเติมเงื่อนไขที่เป็นครึ่งที่แบกน้ำหนัก:
**"มีข้อแม้ว่าทั้งหมดนี้คุณต้องเป็นคนเรียกใช้เองเมื่อต้องใช้"**

---

## 2. สร้างอะไร — เรียงตามความแรงของการบังคับ ไม่ใช่ตามหัวข้อ

| ชั้น | ของ | สิ่งที่มันปฏิเสธ/ทำได้ |
|---|---|---|
| **guard (fail closed)** | `blenderkit.baseline_refusal()` + เรียกใน `fetch()` | paid fetch ในคลาสที่ยังไม่มี free baseline · paid fetch ที่ไม่มี `--assert-class` |
| **agent def** | `.claude/agents/cold-critic-c2.md` | ตาบอดเป็นไฟล์: tool list แคบ + รายการห้ามเปิดเป็นชื่อ |
| **hook** | `pipeline/scripts/skill_usage.py` + settings.local | นับทุก Skill/Agent call · แยก "ไม่มีใครเรียก" ออกจาก "hook ไม่ทำงาน" |
| **skill** | `camera-composition` · `styling-narrative` · `lighting-design` | งานที่เป็นการตัดสินใจ ไม่ใช่การตรวจ |
| **ถอน** | `usd-validate` · `material-lint` → `docs/retired-skills/` | ชี้ไปที่ pipeline ที่มี 0 ไฟล์ `.usda` / 0 `.mtlx` |

---

## 3. การวัด (ทุกข้อ trace แล้ว ไม่ใช่สมมติ)

**ประตูซื้อของอยู่บนทางเดินจริง — พิสูจน์ด้วยการไล่ call graph:**
`download_url()` มีผู้เรียก **หนึ่งเดียว** คือ `blenderkit.py:1087` ซึ่งอยู่ *ใน* `fetch()`
และการปฏิเสธของผมอยู่ก่อนหน้านั้น · ผู้เรียก `fetch()` จากภายนอกมีสองที่ (shortlist:1373,
CLI:1683) ทั้งคู่ผ่าน `fetch()` → **ไม่มีทางลอด**

**สัญญา exit code ยังอยู่ และเทสต์เก่าเป็นคนจับผมเอง:** เวอร์ชันแรกของ guard ยิงก่อน
ASK-031 ทำให้ `test_full_plan_fetch_without_key_exits_2_by_name_and_leaves_no_dir` แดง
(1 != 2) — ไม่มี key = *รันไม่ได้* (2) ต้องชนะ *รันแล้วตอบว่าไม่* (1) เสมอ · แก้ด้วยการ
gate guard ด้วย key

**hook ยิงจริงแล้ว — ในเซสชันนี้เอง ไม่ต้อง restart:**
```
{"ts": "2026-08-27T11:38:28", "kind": "skill", "name": "scrutinize", "session": "b3117b34"}
```
นี่คือการเรียก skill ครั้งแรกที่รีโปนี้เคยนับได้ และมันเกิดจากรอบนี้เรียก `scrutinize`
มาตรวจโค้ดของรอบนี้เองก่อน commit

**เทสต์และ ledger:** 326 tests OK (เพิ่ม 7 blenderkit + 18 skill_usage) ·
`orders_check` exit 0 · `decisions_check` exit 0 · in_effect_assert ทั้ง 12 ข้อของ
D-159/160/161 grep เจอจริง

---

## 4. คำสั่งใหม่ถูกยื่นเป็น **not-obeyed** และเทสต์ของมันคือตัวนับ

ORD-2026-08-27 ถูกยื่นสถานะ `not-obeyed [0d]` ทั้งที่กลไกครบแล้ว **เพราะการเรียกใช้เป็น
พฤติกรรม ไม่ใช่ไฟล์** — และ `obeyed_assert` ข้อแรกของมันคือ:

```
qa/skill-usage.jsonl  ~  "kind": "skill", "name": "(camera-composition|styling-narrative|lighting-design)"
```

วันนี้ยังไม่ match (log มีแต่ `scrutinize`) · **แถวนี้จะพลิกเป็น obeyed ด้วยตัวมันเอง**
ครั้งแรกที่ผมเรียก craft skill ในรอบที่มันใช้ได้จริง ไม่มีใครต้องมาแก้แถว

`orders_check` เองมีกฎว่าแถว not-obeyed ที่ assertion ผ่านหมด = ผิด (มองโลกร้ายเกินจนแถว
ที่ซื่อสัตย์อ่านไม่ออก) — แถวนี้จึงถูกออกแบบให้ **assertion ที่สำคัญที่สุดของมันแดงอยู่**
จนกว่าพฤติกรรมจะเกิด

---

## 5. ข้อจำกัดที่ต้องพูดตรง ๆ

- **hook อยู่ในไฟล์ที่ gitignored.** `.claude/settings.local.json` — มันทำงานบนเครื่องนี้
  เครื่องเดียว clone ใหม่จะนับศูนย์ · `.claude/settings.json` เป็น hard boundary (PR only)
  → **ข้อเสนอถึงพี่ ไม่ใช่ ask row** (เพราะคิว ask ต้องอยู่ ≤6): ย้ายสองบล็อกนี้เข้า law layer
  เมื่อไหร่ก็ได้ที่พี่โอเค ทางเดียวกับที่ `guard_web` เคยเดิน
- **ตัวนับพิมพ์ที่ session open ไม่ใช่ที่ render path.** trace แล้ว: `rule_gate` เรียก
  `PLAN.load/current/progress/review_due` ไม่ได้เรียก `report()` · **ตั้งใจ** — render path
  คือช่องของพี่ (R3) ส่วนนี่คือกระดาษของ builder
- **skill สามตัวเป็นเอกสาร เทสต์ไม่ได้.** หลักฐานเดียวของมันคือ (ก) ถูกเรียกจริง
  (ข) รอบที่ใช้มันทำให้พิกเซลดีขึ้นหรือไม่ · ไม่ถูกเรียก ≥14 วัน = เรียกหรือถอน
- **คลาส `cushion_throw` จะโดนประตูใหม่ปฏิเสธ** ถ้ามีรอบซื้อหมอนก่อนรัน baseline —
  ถูกต้องตามโปรโตคอล แต่จะเซอร์ไพรส์ ข้อความปฏิเสธบอกทางออกสองทางไว้แล้ว

---

## 6. รอบนี้ **ไม่ปิด** work item ไหนใน P2

P2r-28/29/30 ไม่ถูกแตะ · REVIEW ยังไม่ due · นี่คือรอบที่ทำให้รอบต่อ ๆ ไปแพงน้อยลง
ไม่ใช่รอบที่ขยับเป้า — และตามกฎของ plan_status เอง งานที่ทำไม่ได้/ทำแล้วแยกไม่ออกคืองานสองชิ้น
รอบนี้จึงไม่ขอเครดิตจากคิว P2 เลย

**ถัดไปตามคิว:** P2r-28 (ราวแขวนลงบน gable + rung อ่าน load path) — และรอบนั้นจะเป็นรอบแรก
ที่ต้องเรียก craft skill เองตามคำสั่งใหม่

## 7. กลับทางทั้งรอบ

`git revert <sha>` · หรือทีละชิ้น: ลบบรรทัด `gap = baseline_refusal(...)` ใน `fetch()` ·
ลบ `lines += skill_lines()` ใน `plan_status.py` · ลบ hook block ใน settings.local.json ·
`git mv` สอง skill กลับจาก `docs/retired-skills/` · ลบ D-159/160/161 และแถว ORD ใหม่
