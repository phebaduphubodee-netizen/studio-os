# gate-DELIV001-P2r53 — 2026-08-18 — the bed is too small, and he saw it before any instrument did

**THE OWNER'S VERDICT, verbatim (C4, from the p2r52 image):**

> ผมว่าเตียงมันเล็กเกินไป

Filed as `ORD-2026-08-18-bed-too-small` in `qa/owner-orders.json`. An instance
verdict on the staged f52472c1 (R13 class-vs-instance: the CLASS order — the bed
is ACQUIRED, D-104 — stands untouched; what may ship is narrowed).

**IMAGE PAIR** (R2): `pipeline/output/room_bedroom_suite_eye_p2r52.png` (the frame
he judged) vs `_private/deliv-001/whole-bed-audition-2026-08-17/sheet-crop-bed-zone.png`
(the ink of record: a 7'×6.5' made bed, duvet to the edges, filling the zone
between both nightstand blocks).

## The measurement (his eye said WHAT; this is HOW MUCH and WHICH KNOB)

The drawing of record (R12, D-036 snapshot) draws the bed **2149 mm wide × 2000 mm
long (7'×6.5'), h 600, head EAST** — a near-square feature bed, WIDER than a king.

The staged winner, measured off `room_bedroom_suite_eye_p2r52.scene.json`:

| mass | drawn | staged | share |
|---|---|---|---|
| platform incl. integral wings (`bed__frame__acq0`) | 2149 wide | 2149 | 1.00 — the fit made THIS true |
| **mattress** (`bed__frame__acq1`) | — | **1243 wide × 1569 long** | **0.58 of the drawn width** |
| bedding field (`bed__cloth__acq0`) | 2149 (duvet to the edges) | 1541 wide | 0.72 |
| sleeping plane | 600 (spec h) | ~425 | 0.71 |

**WHY THE FIT DID THIS.** f52472c1's platform is ONE mesh whose integral side
wings make it 3159 mm wide native, around a true-king 1827 mm mattress. The fit
denominator (frame cluster) must keep every hard part inside the drawn rect, so
the wings cap the scale at 0.68 — and the mattress shrinks to 1243. No legal
scale fixes it: raising s to give the mattress its width pushes the wings
505 mm past the footprint into the nightstand zone and the walkway; capping by
mattress length (s=0.867) still leaves the wings 591 mm over. **f52472c1 cannot
be the drawn bed at any scale — the same conclusion-shape as D-101's 73% duvet
ceiling, reached one level up.**

**WHY NO INSTRUMENT CAUGHT IT.** The audition's MIN_FILL (0.88) measures the
WHOLE kept bed — wings included — so hard side-furniture carried a narrow bed
through the width test. bed_pixels' ratchet measures BARE flank, which a
smaller mattress trivially improves (a fully-draped small bed out-scores a
made big one). The blind panel judges made-ness, not size. Every green rung
answered the question it was built to ask; none asked about size; his eye did
(R7b's law, fourth instance).

## What this round does

1. `wholebed_rules.made_field_fill` — NEW pure rule + tests: the MADE-BED FIELD
   (mattress + bedding + pillow bank, the soft mass that visually IS the bed)
   must fill ≥ MIN_FILL of the drawn rect on both axes at the staged scale.
   Hard parts stay inside the rect (the existing cluster fit). Regression test
   pinned with f52472c1's real numbers: it is REFUSED.
2. The rule wired into `_place_bed_frame` (fail-loud) and the audition verdict.
3. Re-audition of the cached candidates under the new rule; a fresh SIZE-FIRST
   search of the free tiers ("ชั้นฟรีหมด" has been false twice); winner staged,
   else the row becomes a procurement ask with prices (R13: unbought ≠
   unavailable — the paid tiers are priced and have never been attempted).

## Mid-round ledger (updated as the round runs)

**Re-bench of the 11 cached candidates under the new rule**
(`_private/deliv-001/bed-size-2026-08-18/rebench/`): **f52472c1 now FAILS by
machine** — field 1803×1541 mm = 0.90×0.72 (the live bench reproduces the scene
measurement). Size-passers: `0f56d8b6` (field 0.943×**1.00**, plane 561 ≈ the
signed h600) and `e6bf4690` (0.98×0.885 — but plane 277, half the signed h600;
noted: NO verdict clause checks height, the spec/P2r-24 lane owns it). **Both
already failed the p2r51 blind panel as shipped** — 0f56d8b6 most/partial/most
(its bedding is sheet+throw, no duvet: the white top reads as bare mattress),
e6bf4690 partial×3. So the cache holds NO candidate that is simultaneously
big enough and made enough — the two halves of the drawn bed.

**Size-first search of the free tier** (the queries never asked before —
history shows every bed query was CLOTH-first): 10 queries, 9 hits, 8 fetched.
Dedup: 605409a3 ≡ 149e40a4 (identical extents/prims/bytes — one model under
two ids). 17151c7e "Super King Size Bed 200×200" arrived as a per-primitive
export (56,721 prims — the p2r46 import-killer class) and was flattened:
62.7→6.2 MB, triangles asserted identical (56,897), now `17151c7e_flat`.
Its pre-read is the right construction: a real duvet field 2465 mm wide with a
pillow bank behind — the made-bed grammar at the drawn width.

**Still un-mined free shelf** (declared, not silent): the two showroom SCENES
excluded at p2r51 (08f118fd 30 m, 3360a516 36 m, "mining one bed out is a
project") may hold wide made beds; they are next-in-line free work if the new
batch fails, before any paid ask.

## The finding, in one paragraph

**The drawn 7'×6.5' made bed does not exist on the free tier — measured, not
assumed.** 18 in-room stagings (11 cached re-benched + 7 new from the first
size-first search in the lane's history) + 2 showroom scenes probed by a pure
GLB reader: the widest made-bed field any free candidate can put in the drawn
rectangle is 1,910 mm of 2,149, and every candidate that passes SIZE fails
MADE (0f56d8b6 most/partial/most — sheet+throw, no duvet) or the INK
(603cf98e footboard 379 mm; e6bf4690 plane 277 vs the signed h600). The one
candidate that is MADE (f52472c1, the p2r52 winner) is geometrically incapable
of the size: integral platform wings cap its scale at 0.68 and no legal scale
fixes it. **His verdict therefore converts to a PROCUREMENT ASK with a spec
and prices (ASK-028), per R13: a gap is now a claim about the world, and the
row carries the measurement that makes it one.**

## What is DECIDED (and how to undo it)

- **ORD-2026-08-18-bed-too-small** — filed, NOT-OBEYED [0d], `blocked_by:
  ASK-028` (money/identity: a purchase or a free-tier account only he can
  open). Ages at every session open until the ask clears.
- **D-108** (`qa/open-decisions.json`, obeys the ORD) — the made-bed-field
  rule (MIN_FIELD_FILL 0.88 vs the drawn rectangle) lives in
  `wholebed_rules.made_field/field_fill/field_verdict`, wired into the bench
  (rows without field keys read UNMEASURED, never clean) and into
  `_place_bed_frame` as a three-state: **pass / interim (signed deficit,
  prints loudly) / fail (hard stop)**. Reverse: a bench-passing bed lands in
  `bed_models.frame` and the `field_deficit_signed` key is deleted.
- **INTERIM** — the frame keeps f52472c1 (made, small) rather than swapping to
  a size-passer that would re-open the bare-mattress defect the lane just
  closed (the ratchet's flagship win). The deficit is SIGNED in the spec
  (`bed_models.field_deficit_signed` → D-108 + ASK-028) and prints at every
  build. NOT an opt-out: unsigned deficit = the build refuses to run.
- **Negative controls**: the rule refuses f52472c1 with its real staged
  numbers in the test suite (pinned regression) AND in the live re-bench;
  `field_verdict` without a signature is a hard stop by test.

## Spend (R6)

2 bench runs (11 + 7 candidates, quick renders each) · 1 aborted bench (62 MB
per-primitive import — killed, flattened 56,721→26 prims, re-run) · 1 sweep
(10 queries, 8 fetched, 1 dedup) · 2 showroom GLB probes (pure python, no
Blender) · 1 quick build (R5 proof of the interim path) · 0 full-fidelity
frames (the staging is unchanged — this round's product is measurement +
register state, so no critic pass fires: R7b judges renders, and no new
render exists) · tests: 37 wholebed green (+9 new), orders/asks/decisions
checks 0 violations.

Nothing waits on his signature (R3); he overrules from the image at any time —
and this round exists because he did.
