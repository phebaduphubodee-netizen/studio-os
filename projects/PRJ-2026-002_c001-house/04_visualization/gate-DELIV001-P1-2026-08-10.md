# DELIV-001 — P1 baseline deliverable frame — 2026-08-10 — gate #1

**ภาพ:** ก่อน `pipeline/output/room_bedroom_suite_eye_ld1.png` (2.800 MP) |
หลัง `pipeline/output/room_bedroom_suite_eye_p1a.png` (2400x1800 = 4.320 MP,
full-fidelity, Cycles GPU 256 samples, 8m31s)

1. **แก้อะไร:** เฟรม deliverable เรนเดอร์ที่ความละเอียดของงานส่งจริง (census 658 รูป:
   median 4.194 MP, median aspect 1.250) แทน 2000x1400 ที่ผ่านพื้น D1 มาด้วยระยะ
   0.004 MP โดยไม่มีใครเลือกและไม่มีอะไรคุ้มกัน — และ **scene rows ของมาตรฐานเปลี่ยนจาก
   อ่าน spec มาอ่านฉากที่ build จริง** ซึ่งเป็นการแก้ที่ใหญ่กว่าเรื่องความละเอียดมาก
2. **ผมตัดสินว่า:** PASS เป็น baseline — เฟรมออกมาตรงกับคำทำนายที่ commit ไว้ก่อนเรนเดอร์
   ครบทั้ง 10 แถว รวมตัวเลขทั้งสองตัว (D8=15, D9=13) แปลว่าเครื่องมือทำนายถูก และ P1
   ปิดได้ตามเงื่อนไขของตัวเอง ทางที่ทิ้ง: ไม่แก้ D8/D9 ในรอบนี้ เพราะนั่นคืองานของ P2
   และการไล่แก้ก่อนมีเฟรมฐานจะทำให้ไม่มีอะไรให้เทียบ
3. **ไม่แน่ใจ:** **aspect เปลี่ยนจาก 10:7 เป็น 4:3** — sensor_fit เป็น HORIZONTAL
   ดังนั้น FOV แนวนอนคงเดิม แต่เห็นฝ้าและพื้นเพิ่มขึ้น ~7% องค์ประกอบภาพจึงขยับจริง
   ผมเลือกตามค่ากลางของงานส่งจริง ไม่ได้เลือกด้วยตา — **อยากให้พี่ดูว่ากรอบภาพนี้ยังใช่มั้ย**

**C2 cold critic (R7c): NOT FIRED — บันทึกไว้เป็นหนี้ ไม่ใช่ข้าม.**
ช่องทางของ C2 คือ fresh-context local agent (Agent tool) และ session นี้มีคำสั่งระดับ
harness ห้ามเรียก Agent tool เว้นแต่เจ้าของสั่ง จึงทำตามที่ CLAUDE.md R7c เขียนไว้เองสำหรับ
ช่องทางที่เข้าไม่ถึง: บันทึก ask ที่ค้างไว้ใน bundle README แล้วเดินต่อไป C3 —
ห้ามข้ามเงียบ ๆ และห้ามหยุดเลนเพราะรุ่นนี้รุ่นเดียว
ask ที่เตรียมไว้พร้อมแล้ว: `_private/deliv-001/critique/critique-room_bedroom_suite_eye_p1a/c2-ask/`
(มีแค่ render + PROMPT.md ตาม assert_blind) — **สั่งเมื่อไหร่ยิงได้ทันที**

**C3 (Gemini 2.5 Pro, R7b):** ยิงแล้ว คำตอบเก็บที่
`_private/deliv-001/critique/critique-room_bedroom_suite_eye_p1a/ANSWER_gemini25pro.md`
5 ข้อ triage ครบทุกข้อ:

| id | ข้อกล่าวหา | triage |
|----|-----------|--------|
| C3#1 | เสื้อผ้าในตู้แข็งเหมือนกระดาษแข็ง ไม่มีน้ำหนัก ไม่มีการทิ้งตัว (ร้ายแรงที่สุด) | **✓ รับ → P2a/P2d.** ตรงกับการวัดอิสระ: build log บันทึก `hybrid rail: ... ANALYTIC fallback` 9 ครั้ง และ D8 ชี้ garment 6 ชิ้นว่า box-like (n90 ≤ 6) โดยไม่รู้เรื่อง log เลย **ตาและเครื่องมือเจอสิ่งเดียวกันแยกกัน** |
| C3#2 | หมอนกลมเหมือนลูกโป่ง ผ้าคลุมเรียบแข็งเป็นผืนเดียว | **✓ รับ → P2c/P2d.** วัดยืนยัน: `bed__pillowsoft0/1` n90=52-54 smooth เต็ม — โค้งและ smooth แล้ว แต่ไม่มี seam/piping/แผนที่ผ้า จึงอ่านเป็นสบู่ **D9 มองไม่เห็นข้อนี้เลย** (มันถามว่า flat-shaded มั้ย ไม่ได้ถามว่ามีโครงสร้างผ้ามั้ย) — นี่คือช่องที่มาตรฐานยังว่าง |
| C3#3 | แสง sconce เป็นวงนวลเกินไป ไม่มี hotspot ไม่ทำปฏิกิริยากับร่องระแนง | **✓ รับ → P3.** ยังไม่หักล้างและยังไม่ยืนยันด้วยการวัด: DEBT-15 (emissive mass ต้องสร้าง pool ที่วัดได้) เป็นแถวที่ยัง **ไม่มีเครื่องมือมองเห็น** — ลงเป็นหนี้ที่ P3 ต้องจ่าย ไม่ใช่ข้อที่เถียงได้ด้วยรสนิยม |
| C3#4 | ไม้ตู้ไม่มีลายไม้ ไม่มี texture เป็นแค่สีทึบ | **✓ รับ → P2e (เพิ่มเข้าแผนรอบนี้).** วัดแล้ว: **2 ใน 498 object มี image texture = 0.4%** เทียบงานจริง 50-66% นี่คือช่องว่างที่ใหญ่ที่สุดในเฟรม และ **ไม่มีแถวไหนในมาตรฐานมองเห็นมันเลย** |
| C3#5 | เบาะ bench ตึงเกินไป ไม่มีตะเข็บ ของบนเบาะแข็งทื่อ | **✓ รับ → P2c.** วัดยืนยัน `bench__seat` n90=28 smooth เต็ม 218/218 → ไม่ใช่ปัญหา shading แต่เป็น **ไม่มี seam geometry และไม่มี fabric map** เป็นอาการเดียวกับ C3#2 |

**ไม่มีข้อไหนถูกหักล้าง** และไม่มีข้อไหนถูกรับด้วยรสนิยม — ทุกข้อมีตัวเลขจาก scene dump
หรือ build log กำกับ ข้อที่มาตรฐานมองไม่เห็น (C3#2 บางส่วน, C3#4) ถูกบันทึกว่าเป็นช่องว่าง
ของเครื่องมือ ไม่ใช่ของเฟรม

**RANK (charter finish line):** **NOT ASKED — นับเป็นหนี้.** P5 คือเฟสของ RANK และ
เลนเพิ่งเข้า P2 การยิง `look_bench --blind` ตอนนี้จะเป็นการถามคำถาม P5 ด้วยเฟรมที่ P2/P3/P4
ยังไม่แตะ ซึ่งเปลืองครั้งที่ถามไปเปล่า ๆ (สาเหตุเดียวกับที่ 8 sheet ก่อนหน้าไม่เคยถูกตอบ)

**เข้า knowledge/ รอบนี้:** ยังไม่มีไฟล์ใหม่ สิ่งที่เรียนรู้รอบนี้ถูกเขียนลงในโค้ดที่บังคับใช้มัน
แทน (`scene_dump.py` docstring เก็บวิธีวัดความโค้งพร้อม control 13 ตัว, `rule_gate.
score_exit_policy` เก็บกฎ exit code) — **แต่บทเรียนที่ยังไม่มีที่อยู่คือ "0 จาก reader ที่
มองไม่เห็น ไม่ใช่ 0"** ซึ่งกว้างกว่าไฟล์ใดไฟล์หนึ่ง ควรเข้า knowledge/ ในรอบถัดไป — ลงเป็นหนี้

**Spend:** 3 builds (1 full + 2 quick) · 3 renders · full render 8m31s,
lane wall-clock รอบนี้ ~2h · Gemini 1 call (pro) ·
**Reversible:** yes — เฟรมทั้งหมดอยู่ใต้ `--suffix=p1a` ไม่ทับ control leg;
resolution กลับได้ด้วยการแก้ `camera_config.DELIVERABLE_RES` ค่าเดียว

**คิวที่ผมเสนอ (คำแนะนำ ไม่ใช่คำสั่ง):**
1. **P2e — texture maps** (2/498 = 0.4%) ใหญ่ที่สุดในตัวเลข, C3#4 ชี้ด้วยตา, และ SC-4
   มีการทดลองคุมตัวแปรรองรับว่า roughness/surface ชนะ shadow softness (R .71 vs .39)
2. **P2a/P2d — acquire garments + pillows/shams** C3 ให้เป็นข้อที่ 1 และ D8 ชี้ 6 ชิ้น
   ตรงกัน; garment ที่ sim ล้มกลายเป็นแผ่นแบน 9 ครั้งใน build log เดียว
3. **P2b — tub chair** ตัดสินด้วยกฎ R8 ตรง ๆ **ไม่ใช่ด้วย D8** (D8 ไม่ฟ้องมัน — วัดแล้ว)
4. **DEBT ledger** 15 แถวถึงกำหนดแล้วและยังไม่จ่าย, 9 แถวยังไม่มีเครื่องมือมองเห็น

**ตัดสินรอบนี้:** ดู `qa/open-decisions.json` — รอบนี้ไม่ได้เพิ่มแถวใหม่ที่นั่น
การตัดสินของรอบนี้อยู่ในแผน (`qa/deliverable-plan.json`) ทั้งหมด: P0/P1 ปิด,
P2b ถูกหักล้างด้วยการวัด, P2e เพิ่มใหม่, P4 exit test เขียนใหม่เพราะเดิมวัดไม่ได้,
P4b เพิ่มใหม่ — undo ได้ด้วยการ revert commit 800d22b + 9d28449
