# DELIV-001 — P2 round 44 — 2026-08-16 — gate #33 — **คำสั่งพี่: "ในฐานะ project director แก้ไขเชิงระบบเกี่ยวกับเรื่องนี้ซะ อย่าให้ผิดซ้ำ"** — รอบนี้ไม่ได้แก้ผ้าเตียง มันแก้กลไกที่ทำให้คำสั่งของพี่ถูกกลบด้วยคำตัดสินของผมเอง แล้วค่อยเอาคำสั่งที่ค้างอยู่ไปทำ

**คำสั่งเต็มของพี่ (2026-08-16):**

> ในฐานะ project director แก้ไขเชิงระบบเกี่ยวกับเรื่องนี้ซะ อย่าให้ผิดซ้ำ
> ทำงานยาว ๆ อนุญาติทุกอย่าง รวม DR ด้วย

---

## 1. สิ่งที่ผิด และมันผิดตรงไหนแบบเป๊ะ ๆ

พี่สั่งเรื่องผ้าเตียงไว้สองครั้ง (2026-08-14, 2026-08-15) แล้ว **D-072 ซึ่ง
`decided_by: "builder"` เอา default กลับไปเป็นผ้าที่ปั้นเอง** และรอบถัดมา (r43) ก็ไปนั่งแก้
ตัวเลข tone ของผ้าที่ปั้นเองต่อ ทะเบียนมี 10 แถวที่ตัดสินเรื่องเดียวกัน (r12·r17·r28·r29·
r31·r32·r35·r36·r41·r43) ขณะที่ R8 เขียน stop-loss ไว้ที่ **สอง**

**ไม่มี rung ไหนในรีโปเห็นเลย และเหตุผลเป็นเรื่องกลไก ไม่ใช่เรื่องศีลธรรม:**

> **คำพูดของพี่อยู่ในทะเบียนมาตลอด — อยู่ในช่องที่ไม่มีเครื่องมือตัวไหนอ่าน**

`decisions_check` กฎข้อ 4 ล็อกแถวให้เป็นของพี่ **เมื่อ `owner_override` มีคำพูดของพี่**
D-052 กับ D-054 เก็บคำสั่งไว้ในช่อง `question` แล้วปล่อย `owner_override` เป็น null →
ล็อกไม่เคยทำงาน → `decided_by` เป็น "builder" → แถวนั้นถูกตัดสินใหม่ได้โดยคนเขียนมันเอง

และหลักฐานที่คมที่สุดอยู่ในโค้ดไฟล์เดียวกัน: `build_room.py:1651` เขียนว่า *"the bed cloth
is ACQUIRED, not simulated … owner order 2026-08-14"* แล้ว **บรรทัด 1668 คือ
`_BED_CLOTH_ACQ = False`** — คำสั่งถูกอ้างในคอมเมนต์ 17 บรรทัดเหนือโค้ดที่ไม่ทำตามมัน

## 2. แล้วมันไม่ใช่ครั้งเดียว — fan-out เจอ **แปด** เคสรูปเดียวกัน

รันทีมย่อยไล่ประวัติทั้งรีโปแบบขนาน (คำสั่ง / การกลับคำสั่ง / ask ที่ถูกทิ้ง) ทุกเคสตรวจซ้ำ
กับสถานะไฟล์ **วันนี้**:

| คำสั่ง | สิ่งที่กลบมัน | วันนี้ยังจริงมั้ย |
|---|---|---|
| R11 "ทุกกลไกต้องมองรูปจริง" (08-09) | D-021 ประกาศ R11 **STRUCTURALLY INAPPLICABLE** กับเลนเดียวที่กำลังสร้าง | ใช่ (`rule_gate.py:474`) |
| R1 cap (07-28, กฎบอกว่ามีแต่พี่ที่ขยับได้) | D-021 บอกว่า cap ไม่ใช้กับ room lane แล้ว **ไม่เคยมีแถว DELIV-001 ใน `curriculum-caps.json`** | ใช่ (`grep -c DELIV-001` = 0) |
| build order: มิติ → ของ → แสง (08-08) | P3 (แสง) เปิดและปิดวันที่ 08-11 ขณะที่ P2 (ของ) ยังเปิดอยู่ | แก้ลำดับแล้ว 08-16, ของที่ทำไปแล้วยังอยู่ |
| "ลบ furniture ที่ปั้นเองทุกชิ้น (ยกเว้น BF)" (08-15) | r33 ปฏิเสธ 3 ใน 4 audition ด้วย guard ของเราเอง | ใช่ — `rug` กับ `bed` ยัง `model: None` |
| "สเกลดูแปลก" (08-12) | `_ADULT_SCALE = False` opt-in | ใช่ (`build_room.py:1589`) |
| "สภาพผ้าต่าง ๆ บนเตียงยังดูแปลก" (08-12) | `_DUVET_TUCKS = False` opt-in | ใช่ — แต่เส้นทางทำตามย้ายไปที่การซื้อแล้ว |
| gen-diff "ไอเดียพี่ อนุมัติ ลุย" (08-13) | รัน 2 รอบแล้วหายไปเงียบ ๆ 13 รอบ | ใช่ |
| ผ้าเตียง ACQUIRE (08-14, 08-15) | D-072 | **แก้แล้วรอบนี้** |

**รูปร่างร่วมของทั้งแปดเคส เขียนเป็นประโยคเดียวได้:**

> **คำสั่งที่ถูกทำเป็น opt-in คือคำสั่งที่ไม่ได้ถูกทำ เพราะไม่มีใครพิมพ์ธงนั้น**

## 3. และช่องทางขากลับก็พังเหมือนกัน — **21 ask ที่ถูกทิ้ง**

ไม่ใช่ถูกปฏิเสธ — ถูกทิ้ง audit ของเราเองเมื่อ 08-15 เจอข้อหนึ่งและเขียนประโยคนี้ไว้เองแล้ว —
*"It was not answered and not withdrawn; it stopped being asked"* — **แล้ว audit นั้นเป็น
เอกสาร จึงไม่มีใครมาเก็บ และอีกสี่รอบต่อมาก็ทิ้งเพิ่มอีกสี่ข้อ**

- `owner_questions_carried_forward` ใน scene-graph.json มีผู้อ่าน **ศูนย์** ทั้งรีโป
- `floor2-owner-decision-queue.md` มีอยู่เพื่อถือคำถามของพี่อย่างเดียว ไม่ถูกแตะ **36 วัน**
- คำถาม r19 หดเหลือโทเคน `Q1 · Q2` ใน 8 gate ติดกัน จนคำถามเต็มไม่เหลืออยู่ในไฟล์ไหนเลย —
  **ถึงพี่อยากตอบก็ตอบไม่ได้**
- blind RANK sheet ถูก **ถอดออกจากเส้นชัย** ที่ r34 เพราะไม่มีใครตอบ — มันคือขั้นเดียวที่มองรูป
  และสี่รอบต่อมาพี่พูดว่า *"ในสายตาผมมันยังไม่ใกล้กับคำว่าเสร็จเลย"*
- ชั้น asset ที่เสียเงิน: ปิดเมื่อ 07-12 ว่า DECLINED ด้วยเหตุผล "Budget = ฿0" แล้ว **พี่ยกเลิก
  ฐานนั้นเองเมื่อ 08-01** คำตอบเดิมจึงเป็นโมฆะ แต่หน้าเอกสารยังขีดฆ่าไว้ให้ดูเหมือนปิดแล้ว
  **46 วัน · ศูนย์การซื้อถูกส่งถึงพี่**

## 4. คำพูดของพี่ที่กู้คืนมาจากบันทึก (เก็บไว้ในรีโปตรงนี้ เพราะเดิมมันอยู่นอกรีโป)

> **2026-07-28 (R1–R6):** project นี้ไม่ยั่งยืน เพราะคนทำ (คุณ) มองไม่เห็นว่าตัวเองกำลังทำอะไรและไม่สามารถหยุดสิ่งที่กำลังทำไปในทางที่ผิดไว้ได้

> **2026-08-08 (ลำดับการสร้าง):** ทำแสงตอนของยังไม่ครบ = เอาความผิดของเรขาคณิตไปฝังในตารางแสงกับวัสดุที่มองไม่เห็น — มิติ → ของให้ครบ → แล้วค่อยแสง

*(ประโยค 08-08 เก็บไว้ในบันทึกความจำเป็นใจความ ไม่ใช่คำต่อคำ จึงบันทึกเป็น `commands`
ไม่ใช่ `verbatim` — ทะเบียนคำสั่งปฏิเสธ verbatim ที่ชี้ไม่ได้)*

## 5. สิ่งที่สร้างรอบนี้ — คำสั่งกลายเป็นของที่เครื่องถืออยู่

| ไฟล์ | กฎที่มันบังคับ |
|---|---|
| `qa/owner-orders.json` | ทะเบียนคำสั่ง: คำต่อคำ + ไฟล์ที่คำนั้นยังอยู่ + สั่งอะไร + ทำตามที่ไหน + **assert กับโค้ดจริง** |
| `pipeline/scripts/orders_check.py` | 6 กฎ: verbatim ต้อง reproduce · obeyed_where ต้องมีจริง · **assert อ่านบรรทัดโค้ด ไม่ใช่คอมเมนต์** · builder ห้ามขัดคำสั่งที่ยังมีผล · คำสั่งใน prose ไม่ใช่คำสั่งของเครื่อง · **stop-loss เป็นตัวนับ** |
| `qa/owner-asks.json` + `asks_check.py` | ask มีสามสถานะ ไม่มีสี่: open / answered / **withdrawn พร้อมเหตุผล** · แถวห้ามหายจากไฟล์ (`_count_floor`) · พิมพ์ **อายุเป็นวัน** ทุก gate ทุก session |
| `qa/sourcing-tiers.json` + `sourcing_check.py` | **"ยังไม่ได้ซื้อ" ไม่ใช่ "ไม่มีของ"** — gap ที่ไม่เคยแตะชั้นเสียเงินที่ R8 อนุญาต ถูกปฏิเสธโดยระบุชื่อ และกลายเป็น procurement ask ที่มีราคา |
| wiring | ทั้งสามตัวถูกเรียกจาก `rule_gate.check()` (บล็อก) + `enforce()` (พิมพ์เข้า render path) + `plan_status` (พิมพ์ตอนเปิด session) |

**สถานะที่สาม และทำไมมันไม่ใช่ช่องโหว่:** คำสั่งบันทึกเป็น `not-obeyed` ได้ แต่ต้องมี
`since` (เพื่อให้อายุพิมพ์ออกมา) **และ** หนึ่งใน `blocked_by` (แถว ask ที่ยัง open — ของที่
มีแต่พี่ปลดได้: เงิน, ตาพี่, ลายเซ็น) หรือ `restart_by` (งานฝั่งผมที่ระบุชื่อชัด) — ห้ามเงียบ
และห้ามพิมพ์เหมือนทำตามแล้ว

## 6. แล้วเอาคำสั่งที่ค้างไปทำจริง

1. **`_BED_CLOTH_ACQ = True`** — เป็น default แล้ว ไม่ใช่ธง
2. **fallback ถูกปฏิเสธโดยระบุชื่อ** (`_ACQ_CLOTH_FALLBACK_IS_REFUSED`) — เดิมถ้าวางผ้าที่ซื้อ
   ไม่สำเร็จ มันจะตกกลับไป bake ผ้าที่ปั้นเอง **แล้วพิมพ์บอก** ซึ่งแปลว่าธงเป็น True ได้
   gate เขียวได้ และเฟรมที่พี่ดูยังเป็นผ้าที่ปั้นเอง — คำสั่งถูกทำตามในคำประกาศ และถูกฝ่าฝืน
   ในพิกเซล ตอนนี้ล้มเลย และทางออกที่ซื่อสัตย์คือ `--bed-cloth-gap` = เตียงเปล่า + gap ที่เซ็น
3. **สเปกชี้ `0afd4c6f`** ไม่ใช่ `8635b5b9` — **เพราะพี่ตกตัวนั้นไปแล้วจากรูป p2r41**
   ("ผ้าบนเตียงยังเละอยู่เลย") และนี่คือกับดักที่ r43 เหยียบ: **คำตัดสินราย INSTANCE
   ไม่เคยยกเลิกคำสั่งระดับ CLASS** คำตอบของ "ตัวที่ซื้อมาตัวนี้เละ" คือ **ซื้อตัวอื่น**
   ไม่ใช่กลับไปปั้นเอง · 0afd4c6f คือตัวที่สองจากสองตัวที่ผ่านเกณฑ์วัดได้จาก 68 ตัว
   (need 0.855x ≤ 1.0 · คลุม 85.4% · ตกข้าง 2/4 · relief 79.3 มม.) และ **ไม่เคยถูก render
   ในห้องนี้เลย** ผมเคยตัดมันทิ้งด้วยคำว่า "อ่านเป็นฟอยล์ยับ" จากภาพ thumbnail ซึ่งเป็น
   look judgment ที่ผมไม่มีสิทธิ์ตัดสินแทนพี่ (R7c/R3)
4. **ASK-001 ยื่นเป็นคำถามเรื่องเงินจริง ๆ** พร้อมสเปกที่วัดมาแล้วและราคา — คำถามที่ควรถาม
   ตั้งแต่ r32 แทนที่จะประกาศว่าไม่มีของ

## 7. ที่ยังไม่ได้ทำ และไม่กลบ

- **ผ้าคาดปลายเตียงสีเกรจ์** ยังหายไปบนเลกที่ซื้อ และเหตุผลวัดได้: ชุดที่ซื้อมาตกถึงเส้น
  ปลายเตียงเอง เหลือ 0 มม. ให้ผ้าคาดห้อย (วัดได้ 85–95 มม. proud ทุก rung) → **ประกาศเป็น
  ABSENT พร้อมการวัด** ไม่ใช่หายเงียบ และไม่ใช่เหตุผลให้กลับไปปั้น (ASK-020)
- **คำสั่งที่ยัง NOT-OBEYED** พิมพ์ทุก gate พร้อมอายุ — ดูรายการในผลรัน §8

## 8. ผลรัน + สิ่งที่ตัดสิน (ต่อท้ายเมื่อรันจบ)

**ภาพ:** `pipeline/output/room_bedroom_suite_eye_p2r44.png` (full fidelity, เลกที่ซื้อ
เป็น default) · playblast `..._p2r44_ql.png` · เทียบกับ `..._p2r43.png` (เลก solver ที่
รอบก่อนยังใช้อยู่)

### 8.1 gate พูดอะไรตอนนี้ (เลนห้อง — เลนเดียวที่ผลิตอยู่จริง)

```
RULE GATE (room lane) — what ran and what did not:
  [x] R10 spec                   11 object(s)
  [x] P2r-9 model scale          10 reference(s)
  [x] owner orders               17/20 standing orders assert clean
  [x] sourcing                   3 paid tier(s) permitted, 3 purchase(s) with him
  [x] owner asks                 21 open, oldest 46d, 3 about money
```

สามบรรทัดล่างไม่เคยมีมาก่อนวันนี้ — และเกือบไม่ได้มีเหมือนกัน: ผมเขียนมันลง `check()`
ก่อน ซึ่ง **เลน DELIV-001 ไม่ได้เรียก** (มันเรียก `check_room`) นั่นคือรูปเดียวกับ R11
ที่ถูกประกาศว่า "ไม่ applicable กับเลนนี้" เป๊ะ ๆ — กลไกที่สร้างมาเพื่อหยุดเรื่องนั้น
เกือบตายด้วยเรื่องนั้นเองในวันแรก มีเทสต์ชื่อ `test_the_room_lane_runs_his_orders_too`
กันไว้แล้ว

### 8.2 เฟรมบอกอะไร

- **`cloth_edge` ผ่านแบบไม่ต้องเถียง**: ขอบผ้าทับผ้า **cast เส้นจริง 98.6% ของคอลัมน์**
  (median 34.6 codes, n=74) เทียบ control (หมอนทับผ้า) 95.4% -> **1.034x** ของ control
  ขณะที่ cut อยู่ที่ 0.5 · เลก solver วัดได้ 0.406x และ 8635b5b9 ที่พี่ตกวัดได้ 0.686x
- **`SCORE DELIVERABLE: QUALIFIES — 6/6`**
- **`throw_weave` mask half = N/A BY DECLARATION D-083** (ไม่ใช่ COULD NOT RUN อีกแล้ว)
- **`wood_boards` = PERIODIC (cut broken)** — ของเดิมที่ยังเปิดอยู่ (P2r-2) ไม่ใช่ของใหม่

### 8.3 สิ่งที่การทำตามคำสั่งเปิดโปงออกมา และผมไม่กลบ

1. **บันไดโทนวัดผ้าที่ซื้อมาไม่ได้ 3 ขั้นจาก 7 ทันทีที่ default พลิก** — เครื่องมือถูก
   สร้างมาวัดผ้าที่ปั้นเอง พอของจริงเปลี่ยนคลาส มันเงียบ · แก้ด้วย D-086: แยก
   *เปลี่ยนชื่อเพราะซื้อ* / *หายเพราะการตัดสินที่เซ็นแล้ว* / *วัดไม่ได้จริง ๆ* ออกจากกัน
   และ**ถาม DECLARED_ABSENT หลัง ACQUIRED_AS เสมอ** เพื่อให้ชุดที่ซื้อรอบหน้าที่มีผ้าคาด
   พับจริง ถูกวัด ไม่ใช่ถูกบัง
2. **ผ้าที่ซื้อมา render ที่ 163.5 ขณะที่เป้าของขั้น coverlet คือ 142.0 (+21.5)** และ
   **ไม่มี rung ไหนหยุดมัน** เพราะกฎ acquired-rung ของเราเอง (D-077) บอกว่า point target
   ของ rung ที่ซื้อมาให้ *รายงาน ไม่บังคับ* — ซึ่งถูกในเหตุผลเดิม (เป้าถูก solve บนเรขาคณิต
   ของ solver) แต่ผลคือเฟรมนี้ส่งผ้าที่ห่างจากบันไดที่เซ็นไว้ 21 codes โดยไม่มีใครค้าน
   **นี่คือช่องว่างใหม่ที่เกิดจากการทำตามคำสั่ง และมันคืองานของ P2r-13 ที่ผม re-scope แล้ว**
3. **เตียงอ่านเป็นก้อนซีดก้อนเดียว** — ตรงกับที่สเปกที่เซ็นเขียนเตือนไว้ล่วงหน้าว่า
   ผ้าคาดเกรจ์คือสิ่งที่กันไม่ให้เกิดอาการนี้ · เป็น ASK-020 ไม่ใช่ใบสั่งให้ปั้นผ้าคาดคืน

### 8.4 ตัดสินอะไรไป และกลับทางยังไง

| # | ตัดสิน | กลับทาง |
|---|---|---|
| D-080 | คำสั่งกลายเป็นแถวที่เครื่องถือ + wire เข้า 3 จุด | ลบสามบล็อกใน rule_gate |
| D-081 | `_BED_CLOTH_ACQ = True` + fallback ถูกปฏิเสธโดยระบุชื่อ | ธงเดียว — แต่ orders_check จะตกพร้อมคำพูดพี่ |
| D-082 | สเปกชี้ `0afd4c6f` (ตัวที่พี่ยังไม่เคยเห็น) | เปลี่ยน cloth_set |
| D-083 | ผ้าคาดปลายเตียง = ABSENT พร้อมการวัด ไม่ปั้นคืน | ชุดหน้าที่เหลือเส้นปลายเตียงว่าง |
| D-084 | D-074 ไม่ใช่ gap แต่เป็นการซื้อที่ยังไม่ได้ทำ -> ASK-001 | พี่ตอบว่าไม่ซื้อ |
| D-085 | ทะเบียน ask + อายุ พิมพ์ทุก gate/ทุก session | ลบบล็อก asks |
| D-086 | แยก "ไม่มีเพราะตัดสินแล้ว" ออกจาก "วัดไม่ได้" ในบันได + p2_exit | ลบ DECLARED_ABSENT |

### 8.5 spend

3 full-fidelity + 1 quick playblast · 1 workflow fan-out (9 agents) · 3 ledger + 3 checker
+ 3 test suites (89 เทสต์ใหม่, สวีตรวม 3,196 ผ่าน) · ไม่มีการซื้ออะไรทั้งสิ้น (ASK-001
รอพี่ และเลนไม่ได้บล็อกรออยู่)
