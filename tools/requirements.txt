# PlingPeat AI-access tools — Python deps. Install: pip install -r tools/requirements.txt
# (All four are already present in the founder's current Python 3.12 env as of 2026-07-02.)
python-dotenv>=1.0.0
httpx>=0.27.0
google-genai>=0.3.0      # gemini_dr.py (google.genai) ; gemini_image.py uses stdlib urllib only
openai>=1.40.0           # chatgpt_dr.py (Responses API)
