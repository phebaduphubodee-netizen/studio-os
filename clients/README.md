# Clients — profile + episodic memory (blueprint §4)
clients/<client-id>/profile.md   — consolidated truths (conflict-resolved, latest-wins with provenance)
clients/<client-id>/episodes/    — append-only dated event logs
PRIVACY: see .claude/rules/client-privacy.md. IDs (C-001) in logs, never names.
