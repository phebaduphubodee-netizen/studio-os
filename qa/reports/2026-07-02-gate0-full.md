# Gate 0 full (M3.1) — engine v0.4 → v0.4.1 + adversarial scrutiny

Slice: the scrutiny report's recorded-open items (2026-07-02-scrutiny-019a67f.md
line 146-149) — true rectilinear min-leg-width for carved rooms, corridor/
circulation enforcement (Gate 0 proper) — plus door-swing arcs and the seeded-
violation acceptance for **M3.1: "Seeded violations (blocked swing, 800 mm
corridor, collision) all caught pre-render"** → `pipeline/scripts/test_gate0.py`,
**50/50 green**, prints the acceptance line on success.

## What shipped
- `suite_clearance.py` v0.4 → v0.4.1: maximal-clear-rectangle leg engine (ข้อ 20 /
  ข้อ 21 trichotomies), circulation bottleneck (L∞ erosion + BFS), quarter-disc
  swing arcs, net-room in-bounds, ฉ.39 tiers, ข้อ 22 tiers. Tier doctrine
  everywhere: **statutory PASS proven on the AS-BUILT basis (sub-room wall bands
  carved — build_room extrudes walls outward), statutory FAIL only on the
  charitable raw-outline basis, WARN in between.**
- `dimensional_rules.v0.2.json`: + `office_dining_floor_to_floor_min_mm` 3000
  (cited, WARN-only tier); notes on balcony entry.
- `suite_plan.py`: `_door_swing` now honors the spec's `swing` field with the
  engine's hinge convention — the drawn arc matches the leaf the gate verified.
- `specs/bedroom_suite.json` REVISED (all inside its declared draft-judgment
  zone; each change forced by an engine-proven violation — see spec note):
  ensuite interior 3100→2900 (as-built east leg = 2500 exactly), ensuite door
  east-face→south-face out-swing (east door was physically unreachable, 49 mm
  bottleneck past the bed block), wardrobe 3200→2100 (opens the only ≥ 910 mm
  route), entry door −100 mm (panel pinched entry to ~800), headboard depth
  600→574 (cleared the ensuite's as-built wall band), vanity stool → ensuite
  fixture (old coords sat ON the shower), bench removed (no legal position).
- Verdicts: `bedroom_suite` REVIEW (1 warn) → **PASS 0/0**, now proven as-built;
  `living_condo` stays PASS 0/0.

## Adversarial scrutiny of v0.4 (Workflow, 58 agents)
4 finder lenses → 18 findings → 3 refuters each (2-of-3 to confirm).
**8 confirmed, all fixed in v0.4.1:**
1. **BLOCKER false-FAIL** — ฉ.39 separated tier unloaded: a legal 900×1400 WC
   compartment FAILed against the combined 1.5 m² floor. → tier resolution from
   fixtures; dual-tier honesty when unknown.
2. **MAJOR false-FAIL** — net area by naive subtraction: sub-rooms outside the
   outline / overlapping each other minted statutory FAILs. → exact grid net.
3. **MAJOR false-FAIL** — condo corridor < 1500 stamped "breach under every
   reading"; the vault ties 1500 to COMMON corridors — in-unit condo scope is a
   recorded vault gap. → WARN naming the question (open codes-th research task).
4. **MAJOR false-WARN** — wet-typed MAIN rooms escaped ฉ.39 entirely and drew a
   wrong-clause ข้อ 22 WARN; `_WET` missed ส้วม/อาบน้ำ/powder. → fixed both.
5. **MINOR false-FAIL** — ข้อ 22 is a tier table: ระเบียง 2200 (was FAILed at
   2600), สำนักงาน/ห้องอาหาร 3000 (now WARN-only, in-unit interpretation open).
6. **BLOCKER false-PASS** — zero-thickness sub-rooms: build_room extrudes walls
   OUTWARD, so v0.4's "leg 2500" PASS was physically 2400, and the headboard
   overlapped the ensuite wall band by 26 mm undetected. → as-built keep-out
   basis everywhere + spec re-revised to prove 2500 as-built.
7. **BLOCKER false-FAIL** — circulation start pinned to the doorway centre: an
   entry console off-centre zeroed the bottleneck. → multi-start across the
   doorway + line-of-sight reach (also kills "reached through a thin wall").
8. **MAJOR false-FAIL** — platform treated as pure obstacle made podium beds
   unreachable. → bed target = bed ∪ its low platform.

**Rejected:** 1 refuted (bedroom-regex over-trigger — behavior is the disclosed
heuristic tier), 2 duplicates of confirmed findings from other lenses.
**Unverified-by-infrastructure (7)** — the verify fleet hit the session limit
mid-run (see degradation note); each was re-verified inline by probe and fixed
where real: through-wall reach (real → LOS fix + seed), rot 90/270 (real → bbox
swap + seeds), suite_plan swing-field mismatch (real → fixed), corridor+bed
shadowing ข้อ 21 (real → independent branches + seed), swing vs the room's own
boundary (real → WARN tier + seed), missing sub-room-door seeds (added), doorway
width as envelope cap (deliberate non-goal — doors are permitted pinch points
with their own 800 studio floor; disclosed in docstring + footer).

## Infrastructure degradation (disclosed)
- `find:geometry` died on a network error and never ran — the dedicated
  pure-geometry attack lens is a RESIDUAL GAP, partially mitigated by the
  50-seed suite (plus-shape, donut, niche, L-shapes, exact-kiss widths) and by
  geometry probes the other three lenses ran. Re-run that lens before the next
  engine-heavy slice.
- 29/58 verify agents hit the account session limit; unverified ≠ refuted —
  all 7 affected findings were re-verified inline (above) instead of dropped.
- Workflow script bug (ours): finder results were `filter(Boolean)`ed before
  zipping with lens names, so surviving findings carry shifted lens labels in
  the raw output. Content unaffected.

## Remaining open (recorded, not in this slice)
- Turning circles + knee/toe clearances — need catalog seat/desk metadata (M3.1
  contract lists them; engine footer discloses).
- `clearance_check.py` unification (still IRC/inch).
- codes-th research: does ข้อ 21's 1500 tier bind IN-UNIT condo corridors?
  Does ข้อ 22's 3000 tier bind in-unit home-office/dining spaces?
- Envelope vs doorway width: deliberately not coupled (disclosed).
