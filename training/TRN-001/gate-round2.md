# TRN-001 / element 2: joinery — 2026-07-31 — gate #2

**ภาพ:** ก่อน `renders/trn001_blockout_v003f.png` (โครงเปล่ารอบ 1) | หลัง `look/trn001_blend_r2gate.png` (blend 50/50) + `look/trn001_pair_r2gate.png` + `look/trn001_edges_r2gate.png` + เฟรมเต็ม `renders/trn001_blockout_r2gate.png`

1. **แก้อะไร:** ใส่งานช่างไม้ที่วัดมาจากภาพจริงทั้งชุด — ตู้กลายเป็น carcass เปิด 5 ช่องต่อข้าง (ladder 428mm ที่ **ทั้งสองตู้ใช้ร่วมกัน**), header แตกเป็น 3 แผงมีร่องต่อ + กรอบทองเหลือง, plinth เป็นบานลิ้นชัก 5 บาน **513mm เท่ากันเป๊ะทั้ง 5** คั่นด้วยร่องเงาที่มีก้นร่อง (ไม่ใช่ช่องทะลุ) บนฐาน toe เว้า, ฐานตู้ขวาเป็นแถบ skirt, มุมโค้งของแท่นบูชาขยายเข้าตระกูล 110-170mm ตามที่ critic วัด, ผนังห้องปิดชิดขอบตู้ที่ x=-1608
2. **ผมตัดสินว่า:** ผ่านระดับ rung — **CONFIDENT metric 7.9px / median 4.7px** (จาก 9.5px รอบ 1) โดยเปลี่ยนวิธีวัดให้ซื่อขึ้นด้วย: landmark ที่อ่อน (ปลายโค้ง silhouette) ถูกลดน้ำหนักเหลือ 0.12 และแทนด้วยจุดที่ probe fit ได้ ≤0.4px (ขอบบนหน้า plinth ตัดกับร่องลิ้นชัก 4 จุด) — ตัวเลขที่รายงานจึงมาจากจุดที่วัดได้จริงเท่านั้น
3. **ไม่แน่ใจ (ต้องให้คุณเคาะ):** **ตู้ลึกเท่าไหร่กันแน่** — เครื่องมือสองตัวขัดกัน 4.5 เท่าและผมแก้ไม่ตก: (ก) landmark fit มีก้นบ่อชัดที่ **360mm** (300→12.0px, 360→10.6, 450→13.4) แต่ (ข) แถบข้างตู้ซ้ายที่ตาเห็น — critic วัด target ได้ **28px เรา 101px** — บอกว่าลึกแค่ ~80mm ผมลองแก้ทาง (ข) สองครั้งแล้วเฟรมแย่ลงทั้งสองครั้ง (สร้างแผงขาวยื่น → กลายเป็นซุ้ม TV ลึกที่ target ไม่มี / ลดเหลือ 80 → metric พุ่ง 6.4→27px) จึง **หยุดตาม R1** และคงไว้ที่ 360 พร้อมบันทึกข้อขัดแย้งไว้ทั้งหมด

**C2 cold critic (R7):** 7 ข้อ ทุกข้อมาพร้อมพิกเซลที่วัดเอง (critic เขียน python probe เทียบสองภาพ) — critic ยังยืนยันเองว่า **กล้อง/ช่องชั้น/ระดับขั้นตรงแล้ว** (niche 668 vs 672px, ขอบห่าง 3-7px, ช่องชั้นสูงตรงกันทั้ง 5+5)
· ✓รับ+แก้แล้ว 5: มุมคมทั้งชุด→โค้งตามที่วัด (critic เลือกเป็นข้อสำคัญที่สุด) · ตู้ขาวไม่มีตีนตู้→toe 16/45 · ตู้ซ้ายลอยเห็นก้น→ลงถึงพื้นให้ plinth ที่ลึกกว่าบัง (กลับคำตัดสินรอบ 1 ที่ว่า "ตู้ซ้ายตั้งบน plinth" — เฟรมหักล้างแล้ว) · แท่นเลื่อนขวา+อ้วน→วัดใหม่บนระนาบ silhouette ที่ถูก (กับดักเดิมของรอบ 1 ซ้ำรอย: ของที่มุมโค้ง เงาขอบไม่ได้อยู่ที่ระนาบหน้า) · ไม่มีฝ้า/ห้อง→ผนังปิดที่ x=-1608 ซึ่ง back-project คงที่ทั้ง 3 จุด
· ⏸ ยกเป็นคำถาม gate 2: ความลึกทั้งชิ้น (ข้อ 4+7 ของ critic คือเรื่องเดียวกัน และตรงกับข้อ 3 ของ critic รอบ 1 ที่ผม triage ผิดไปว่า "ลดความลึก header พอ" — คลาสนี้โผล่ 2 รอบติด)
· ⏳ ค้างไว้ตรงๆ: ช่องล่างสุดของตู้ซ้ายในของจริง **เตี้ยกว่าอีก 4 ช่อง** เพราะ plinth มาตัด (ของเราเท่ากันหมด) · ขอบ**บน**ของแท่นเป็นสันคม ของจริงมนแบบ bullnose (คนละเรื่องกับรัศมีในผัง)
**C3:** not fired — ยังเป็น rung ระหว่างทาง

**Spend:** full 2048 = 2 เฟรม (r2f, r2gate) + quick 6 · solve ฟรี ~12 รอบ · probe fleet 4 agents ~629k tokens + critic 1 ตัว ~169k · สะสมทั้ง lane: full 5, quick 10
**Reversible:** yes — spec/landmark/test เป็นไฟล์ทั้งหมด, กลไก joinery เป็น opt-in (ลบ key ในสเปกก็กลับเป็นโครงรอบ 1)
**ขอ verdict:** ไปต่อ (รอบ 3 วัสดุ) / ตอบคำถามความลึกก่อน / แก้ตามนี้ / ฆ่าทิ้ง — พร้อมให้ตัดสิน
