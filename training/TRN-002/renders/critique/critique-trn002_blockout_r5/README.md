# Bundle — trn002_blockout_r5 (round 5 frame of record)

Render: `_private/…/renders/critique/critique-trn002_blockout_r5/trn002_blockout_r5.png`
Change from r4b: the duvet only — widened 330→540, crest raised to 710 (inside
the measured band 626..734), rolled `axis:"y"` so the crest is section-round,
hem thickened and bulged to 10mm shy of the platform-edge plane the target's
roll bulges to.

## C2-r4b triage (the three remaining items, closed with counts and fits)

1. **ดาวน์ไลท์ ≥5 vs 4 — CLOSED BINARY, C2's own prescription.** Blob scan of
   the TARGET FILE's ceiling strip: exactly 4 compact peak-255 blobs, at the
   4 M positions (199,13)(227,111)(547,138)(662,66). The other brights are the
   shelf-lighting strip (31×2 elongated) and blind/niche highlights at u~61.
   Spec masses: exactly 4. The ≥5 counts came from OUR render's clay-light
   artifacts (bright pool frame-left + dl speculars) — a LIGHT-phase debt,
   named in spec `downlight_table`. No fifth round for this item.
2. **เก้าอี้ [verify] — CLOSED with the file + one new fit.** Seat z 0..450,
   back 450..780×80, feet M twice. And the target's own chair back reads at
   v≈490 → z≈770 on its plane ≈ our 780. Bonus: the "two-level desk" hypothesis
   this reopened was MEASURED and refuted — two windows of the v~525 top edge
   fit ONE line (v(110) differs by 0.1 px between windows); the vertical
   gradient at u 102-106 is the vase silhouette, not a step face. Desk = one
   level, 875, as adjudicated in r3.
3. **ผ้านวมแทบไม่มีมวล — ACCEPTED, and it triggered R1.** See below.

## C3-r5 triage

1. **"ผ้านวมยังเป็นท่อนซุง ไม่ใช่ผ้าพับ"** → ACCEPTED — and this is the SECOND
   post-fail fix-shape on the same object (r3b L-drape, r5 roll). **R1
   stop-loss fires: a third shape means the class was misclassified — and it
   was. A folded duvet is free-form DRAPERY, the exact class R8 forbids
   hand-modelling.** Clay iterations on the duvet HALT at the measured
   envelope (crest 710 in band 626..734, extent 540, hem wrapping the foot to
   the ledge). The "reads as folded fabric" requirement transfers to the
   CLOTH phase, whose machinery TRN-001 already proved (headless sim,
   deterministic, 0.48 s). Stopping is the correct move, not an admission.
2. **"เก้าอี้เด็ก"** → REFUTED with the numbers in item 2 above — the
   proportion (seat 450 / back 780 / desk 875) is the target's own. The thin
   80mm back slab under clay light is a legibility residue → materials.
3. **"nightstand ตั้งพื้น"** → SIXTH blind-critic misbind of petcave. Same
   refutation (floats 311..481, M). The ticket's evidence file grows.
4. **"ทุกอย่างมนเกิน (ดินน้ำมัน)"** → split: measured cuts stand (platform 300,
   console 55 — target's own corners); unmeasured cuts (chair 180, bench 200,
   petcave 250) are declared A and get their pass with materials/styling.
   Partial-accept as polish notes, no rebuild.

## Ladder state

- C1: foot-zone triptych target|r4b|r5 — the fold has mass now; the roll read
  is real and is the clay ceiling for this class (see R1 halt above).
- **C2 (Cowork, R7c): PENDING** — attach `trn002_blockout_r5.png` + this
  bundle's `PROMPT.md`; archive here as `ANSWER_claude-cowork.md`.
- C3 (Gemini): filed, triaged above.
