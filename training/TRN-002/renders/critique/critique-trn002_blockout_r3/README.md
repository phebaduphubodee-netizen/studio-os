# Bundle — trn002_blockout_r3 (round 3, frame of record)

Render: `_private/…/renders/critique/critique-trn002_blockout_r3/trn002_blockout_r3.png`
(binaries under `_private/`, text here — post-split tree.)

## Ladder state

- **C1 (builder LOOK)**: blend sheet `trn002_blend_r3.png` — clay masses ride the
  target's own lines (platform edge, duvet, pillow band, band top, console/desk
  run, chair, lamp). Blockout scope met.
- **C2 (Claude-Cowork, R7c): PENDING** — ask when convenient: attach
  `trn002_blockout_r3.png` + this bundle's `PROMPT.md` in the Cowork chat,
  archive reply here as `ANSWER_claude-cowork.md`. Lane proceeds per R7c
  (never blocks on this rung alone).
- **C3 (Gemini 2.5 Pro)**: `ANSWER_gemini25pro.md` filed 2026-08-04, live API.

## C3 triage (R7 — accept+lane or refute WITH measurement)

1. **"เตียงเป็นแท่นบูชาหลายชั้น / ยังไม่แก้"** → geometry REFUTED with M numbers:
   platform top 221±4 (M, x=-3555), ledge 165 (M, both faces), mattress 520,
   duvet 675 — and the r3 landmarks land on the target's own pixels to <0.01 px
   (plat_top_foot, mat_foot_contact). The blend sheet shows the clay edges riding
   the target's. What the eye correctly reads as rigid = hard clay steps where
   the target has wrapped fabric + shadow → **ACCEPT residue into the
   materials/cloth phase**, not another geometry pass.
2. **"partition กระจกกลายเป็นบานทึบ"** → ACCEPT-clay: glass is a material; the
   closet behind renders dark in clay. Lane = materials/light phase.
3. **"nightstand เป็นกระบอกตั้งพื้น"** → REFUTED with scene: nightstand floats
   z 311..481, in frame [964..1080-clip]; the standing round thing is `petcave`
   (a real target object, identity open). Same misread C2 made on r2b — the
   clay tone-merge is the cause → materials phase.
4. **"เสากระบอกแปลกปลอมซ้ายประตู"** → identity REFUTED: it is `shelf_col`, in
   program ("ตู้ชั้นโค้งที่มุมห้อง") — but the underlying complaint is the one
   C2-r2b #4 already earned: **the voids are still owed** (the shelf/slot
   measurement agent died this round) → round-4 queue #1.
5. **"ไม่มีเก้าอี้"** → REFUTED with scene + two independent M fits of its feet
   (r2 + r3 desk agent); chair box [160-267, 487-628] renders but tone-merges
   with the desk band → legibility, materials phase.
6. **r2b symptom check**: โต๊ะแผ่นบาง+แผ่นข้างลอย **FIXED** ✓ · คอนโซลถังตัน
   **FIXED** ✓ (both GATE #2 owner flags closed by the cross-vendor eye);
   the anonymous capsule is GONE (pillows now read as pillows — the complaint
   that replaced it is #1 above, a different, clay-phase item).

R7b pairing note, written down because this round is the cleanest case yet:
both of C3's "ยังไม่แก้" verdicts collide with M measurements that pin the clay
to the target's own pixels. The eye stays the authority on "reads wrong" — and
it DOES read wrong — but the instruments say the wrongness now lives in
material/cloth/light, not in mass placement. That is what "blockout done"
means, and no self-judgment could have established it.
