# Bundle — trn002_blockout_r4b (round 4 frame of record)

Render: `_private/…/renders/critique/critique-trn002_blockout_r4b/trn002_blockout_r4b.png`
r4 → r4b: one geometry bug — the shelf-column's niche BACK panel shipped on the
CAMERA face (y −400..−340), filling both voids from view; C3 caught it on the
r4 pass while the builder's LOOK was parked on the head zone (habituation, the
exact mechanism C2-r3b named). Moved to the north face y −60..0.

## Round-4 changes (from the gate-#3 fail + C2-r3b triage)

- **Head trio rebuilt as the program's FORMS, not just its names**: pillows now
  LEAN (tilt 20° about their bottom-back edge, tops meeting the headboard face)
  — builder gained `tilt_deg`; bolster is now a LYING CYLINDER (`axis:"y"` —
  oct rounding moved into the x-z section). The r3b capsule was pillow_L's
  end face at 90°; posture is geometry, and the roundness plane is geometry.
- Chair split seat 450 + back 780 (identity was chair all along — M feet twice —
  but one solid box to 780 denied the knee void; C2's function claim accepted).
- Shelf column opened: voids z 818..1331 and 1350..1918, edges backprojected
  from target pixels on face plane y=−400 (A-swept).
- Pinned in spec so third-round items stop recurring: `downlight_table`
  (EXACTLY 4 fixtures, all M, with the non-fixture brights named) and
  `console_float` (gap 495, closed).
- PROMPT addendum DE-LICENSED: the zone-specific "หมอนเป็นก้อน clay แข็ง"
  sentence is gone — C2-r3b showed it worked as a licence to not-doubt every
  strange mass in the pillow zone. The phase statement is now global and says
  the opposite: อย่าอภัยทรง/จำนวนด้วยเหตุผลว่าเป็น clay.

## Ladder state

- C1: head-zone triptych (target | r3b | r4) — capsule and staircase gone; the
  trio reads as two leaning pillows + a roll at first glance.
- **C2 (Cowork, R7c): PENDING** — attach `trn002_blockout_r4b.png` + this
  bundle's `PROMPT.md`; archive reply here as `ANSWER_claude-cowork.md`.
- C3 (Gemini): r4 pass caught the shelf-back bug (its job, done); r4b pass
  confirms **all four gate-#3 items fixed**.

## C3-r4b triage (R7)

1. **"ฐานเตียง ziggurat สูงเกิน 0.22"** → REFUTED with M pins (platform top
   221±4 on x=−3555; the "tiers" are platform ledge 165 + mattress + duvet,
   each measured from the target and landing on its pixels ≤0.01px). Rigid
   read = clay → materials/cloth lane, unchanged.
2. **"nightstand ตั้งพื้น"** → REFUTED — the FIFTH blind-critic misbind of
   `petcave` (identity-open target object) as the nightstand (floats 311..481,
   in frame, M). The recurrence count IS the materials-phase legibility
   ticket's evidence.
3. **"bench ควรเป็นม้านั่งยาว ไม่ใช่ ottoman"** → target wins: the delivered
   image's own object is a rounded ottoman with a fur throw at the measured
   (−2738,−5182) — program word updated next PROMPT (reproduction judges
   against the target, not our own description). Facet note: oct SEG=6 at
   this size — polish item, not this phase.
4. **"bolster สั้นไป ควร ≥ ครึ่งเตียง"** → target wins: the reference's own
   bolster is a short roll at the far corner (~450); norms lose to the
   delivered image (R4b: anchors judge, generic taste does not dictate).
5. **"กรอบรูปเป็นจัตุรัส"** → REFUTED with the landmark table: art corners are
   M and land at target pixels (err ≤0.14px), projected box 95×141px —
   portrait 0.67, exactly 727×1008 under this camera. Second recurrence of
   this misread; the numbers are pinned here to close it.

Blockout scope after r4b: every geometry item from the failed GATE #3 is
fixed and cross-vendor confirmed; every open critic item is either refuted
with a measurement above or lodged with the materials/cloth/light phase.

- `ANSWER_claude-cowork.md` — C2 (R7c) filed 2026-08-04: หัวเตียงปิดแล้ว (หมอนเอียง2+bolster ครบ trio, แคปซูล/บันไดหาย, ตู้โค้งเปิดช่องแล้ว); เหลือ: ผ้านวมปลายเตียงแทบไม่มีมวล (อันดับ1), ดาวน์ไลท์ ≥5 vs 4 (รอบที่สี่ — ตอนนี้ binary), เก้าอี้ติด [verify] มิติ/การมองเห็น
