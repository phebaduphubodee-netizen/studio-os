# TRIAGE — ANSWER_claude-cowork.md (C2, R7c) — trn002_blockout_r2b

- Rule: R7 — ทุกข้อ accept+lane หรือ refute พร้อมตัวเลขที่วัดได้ ห้ามตีตกด้วยรสนิยม
- Instruments: projection ของทุก mass ผ่านกล้อง solved (trn002_geom, spec_r2.json),
  zoom crops ของ render + target (local), reference-board-dimension-norms-2026-08-04.md
- Filed: 2026-08-04, builder session (b8d81872)

## #1 มวลแคปซูลหัวเตียง → ACCEPT (identity แก้ให้แล้ว) — lane: r3

**Identity จากไฟล์ฉาก (การทดสอบที่ C2 ขอเอง):** มันคือ `pillow_L` — placeholder
หมอน (prov `A(placeholder to disambiguate headboard orientation)`), oct 180×650×420
ที่ (−1720,−3400,700) มองเห็น "ด้านสกัด" (end-on) → เสาโค้งแคบ. Projection box
[715–826, 490–600] ตรงกล่อง pixel ของแคปซูลที่ C2 วาดพอดี.

- สมมติฐาน C2 ทั้งสองข้อตกด้วยไฟล์ฉาก: ไม่ใช่ "ก้อนกลมช่องโค้ง" (petcave
  projects [1001–1513, 653–1015] = มุมล่างขวา) และไม่ใช่มวลนอกโปรแกรม.
- ตา-ถูก-เรื่อง-อะไร เครื่องมือ-แก้-เรื่อง-เท่าไร (R7b): มันยืนบนที่นอน z 490..910
  (สูง 420) ไม่ใช่ตั้งพื้น 1.4–1.5m — clay แบนทำให้อ่าน depth ผิดว่าไปยืนหน้าแผงตู้.
- **Lane r3:** แทน placeholder ทั้งคู่ด้วยมวลหมอนแบบนอน (เตี้ย กว้าง แนวนอน
  ตาม target: หมอนพิง 2 + bolster 1) — งาน disambiguate ของมันจบแล้ว
  (assembly หัวเตียงถูก anchor ด้วย band+ตู้ตั้งแต่ r2).

## #2 nightstand หาย → REFUTE ด้วยไฟล์ฉาก+crop+target

- nightstand มีจริงใน spec, prov `M` (arris วัดจาก target ตอน r2): projects
  [964–1210, 631–738] → **อยู่ในเฟรม** โดนขอบขวาตัดที่ u=1080; ลอย z 311..481.
- Crop ขวาล่างยืนยัน: ผิวบน+หน้าชิ้นเรนเดอร์อยู่ที่ขอบขวา ตำแหน่งเดียวกับ
  nightstand ไม้ใน target (ชิ้นเดียว ฝั่งใกล้ มีนกดำวางบน).
- ฝั่งไกล: เฟรม target เองไม่มี nightstand ให้เห็น → reproduction ไม่ติดหนี้ชิ้นนั้น.
- ผิวบน 481 (วัด) vs norms 500–650: measured beats norm — คงไว้.
- **Residual ที่รับ:** ความอ่านออก — ใน clay มันละลายรวมกับ bench/petcave
  → เฟสวัสดุ/แสง (AO, ค่าไม้ vs ผ้า) ไม่ใช่เรขาคณิต. ไม่มีการแก้ตำแหน่ง.

## #3 เตียงยังสูง + ฐานยื่นเป็นชาน → ACCEPT แกนกลาง — lane: r3 คิวอันดับ 1

- แก้เลขก่อน: ที่สร้างจริงผิวบนที่นอน = **550** (แท่น 300 + ที่นอน 250) ไม่ใช่
  ~650–700 ที่ตาอ่าน. แต่ (c) ของ C2 ถูกเผง: **ทั้งสองเลขเป็น A-provenance —
  ไม่เคยวัดจาก target เลย** — นี่คือข้อค้างอันดับหนึ่งของ C3 ด้วย.
- Norms board Class 1: ช่วง US 560–810 → 550 ของเราอยู่ปลายล่างแล้ว; norms
  ถอดความตกใจออกจากตัวเลข แต่ผู้ตัดสินจริงคือ target.
- ชานยื่น: แท่น 2065×2200 vs ที่นอน 1915×2000 = ยื่น 75/ข้าง ที่ z=300.
  ใน target ฐานอ่านเป็นเงาหุบใต้ที่นอน ไม่ใช่ชานสว่าง.
- **Lane r3 (คิว 1):** วัดจาก target บนระนาบเตียง — ผิวบนแท่น, ผิวบนที่นอน,
  รูปฐาน (ยื่น vs หุบ กี่ mm). เลขของ C2 (top ~500–550, recess 60–100) เป็น
  สมมติฐานให้ทดสอบ ไม่ใช่ค่าให้ adopt.

## #4 ชั้นโค้งตัน → ACCEPT — lane: r3 ผนังซ้าย

- ไฟล์ฉากยืนยัน: `shelf_col` เป็น oct ตันก้อนเดียว 408×400×3040. Target โซนนั้น
  เป็นชั้นเปิดมีไฟหิ้ง. กฎของ C2 รับเต็ม: **ช่องว่างคือเรขาคณิต ไม่ใช่วัสดุ** —
  ระนาบชั้นต้องอ่านได้ตั้งแต่ clay. ระยะช่องวัดจาก target ตอน left-wall pass
  (norms 250–350 [practice] เป็น fallback ที่ยังไม่มี source).

## #5 ดาวน์ไลท์ 6–7 vs 4 → REFUTE ด้วยการนับ

- ไฟล์ฉาก: มี 4 ดวงพอดี (`dl_1..4` ทั้งหมด prov M — วัดจาก blob grid ของ target).
- Crop แถบฝ้า (v 0–200): จุดสว่าง **4 จุดพอดี** ที่ตำแหน่ง projected
  (194,8) (224,108) (551,135) (662,62). เฟรม target ก็ 4. นับ 6–7 reproduce
  ไม่ได้ — C2 เปิดช่อง refute จากไฟล์ฉากไว้เอง ใช้แล้ว. ไม่มีการแก้.

## #6 identity ม้านั่ง/คอนโซล → REFUTE ด้วยไฟล์ฉาก+การวัด (รับ 1 lane ข้อความ)

- กล่องยาวซ้ายล่าง = `console`: z 150..520 **ลอยตามโปรแกรมจริง** — ใต้ท้อง
  span v 821..1026 คือ**พ้นขอบล่างเฟรม (821)** ช่องลอยจึงเรนเดอร์ไม่ได้
  โดยเรขาคณิตของเฟรมเอง (target ก็โดนตัดแบบเดียวกัน). ไม่แก้.
- ก้อนกลมกลางล่าง = `bench` ที่ (−2738,−5182) prov M (backprojection ผิวบน
  z=420 จาก target ตอน r2) — ตำแหน่งมาจาก target; บรรทัดโปรแกรม
  "ข้างเตียงฝั่งหน้า" คือข้อความค้างที่ r2 วัดหักล้างไปแล้ว. Target ชนะข้อความ.
- ก้อนขวาล่าง = `petcave` = "ก้อนกลมช่องโค้ง" ของโปรแกรม (identity เปิด) —
  คนละชิ้น ไม่มี duplicate. ช่องโค้งเป็นหนี้เรขาคณิตรอบท้าย ๆ (มุมเฟรม).
- **Lane ที่รับ:** r3 อัปเดตข้อความโปรแกรมใน PROMPT.md ให้ตรงสถานะที่วัดแล้ว
  (bench/console/petcave) — เลิกป้อนบรรทัดค้างให้ critic ทุกรุ่นเจอซ้ำ.

## Q3/Q4 — รับทราบ ไม่ต้อง triage

- Q3 (ความมืดหลัง partition, ช่อง slot ดำ, ไฟหิ้ง, ผ้า, exposure) ตรง phase map:
  แสง/วัสดุ ไม่ใช่เรขาคณิต — คิวหลัง blockout ปิด.
- Q4 ยืนยันของจริง r2: เกาะหาย, มุมโค้งมา, แท่นทุเลา-ยังไม่ปิด (→ #3).

## คิว r3 หลัง triage (เรียงแล้ว)

1. วัด bed stack จาก target (C2#3 + C3 residue) — แท่น/ที่นอน/รูปฐาน
2. หมอน placeholder → หมอนนอนจริง + bolster (C2#1)
3. band-top refit ขวาของ headboard (C3 residue, u 1020–1080)
4. ผนังซ้าย: เปิดช่อง shelf_col (C2#4) + desk/คอนโซล/TV เทียบ target
   (TV เช็ค norms Class 3 ด้วย — C2 r1 flag ยังเปิดใน spec)
5. slot contents (ช่องดำ — เช็คว่ามีเรขาคณิตค้างก่อนโยนให้เฟสแสง)
6. G1 gauge sweep + สคริปต์ score judge_lines (ยังไม่เป็นไฟล์)
7. PROMPT program text refresh (C2#6)
