# Bundle — trn002_blockout_r3b (round 3 frame of record: duvet reshaped)

Render: `_private/…/renders/critique/critique-trn002_blockout_r3b/trn002_blockout_r3b.png`
Same geometry as r3 except the duvet: the owner (C4) read r3's `duvet_roll` as
"ก้อนอะไรไม่รู้ที่ปลายเตียง" — the SAME defect class as the r2b pillow capsule,
second occurrence, this time by the builder's own hand one commit after triaging
the first. Reshaped into a fold that wraps the foot face and rests its hem on
the platform ledge (`duvet_top` + `duvet_drape`, parented as one duvet).

**Law earned (2 occurrences, ledger):** a clay placeholder for soft goods must
READ AS ITS CLASS from the record camera, or it leaves the frame until the
cloth phase. Every new mass answers "what would a stranger call this?" before
it renders.

## Ladder state

- C1: foot-zone triptych (target | r3 | r3b) — the anonymous shelf-block is
  gone; the fold now wraps the end like the target's.
- **C2 (Cowork, R7c): PENDING** — attach `trn002_blockout_r3b.png` + this
  bundle's `PROMPT.md`; archive as `ANSWER_claude-cowork.md` here. (r3's ask is
  superseded by this frame, same as r1→r2b precedent.)
- C3 (Gemini 2.5 Pro): `ANSWER_gemini25pro.md` filed 2026-08-04.

## C3 triage (R7)

1. **"ฐานเตียงสูงหลายชั้นเหมือนแท่นบูชา"** → geometry REFUTED with M pins
   (platform 221±4, ledge 165 both faces, landmarks on target pixels <0.01px;
   the blend sheet shows the edges riding the target's own lines). The rigid
   multi-tier READ is real — and it is clay: the target's identical stack is
   wrapped in fabric and shadow. Residue lodged with the materials/cloth phase.
2. **"ผ้านวมใหญ่ผิดส่วน แข็งเหมือนหิน"** → split: crest 675 sits mid of the
   MEASURED band 626..734 (A-swept, bedstack agent) — height refuted; the
   FOOTPRINT (330 along-bed, full width, hard edges) is A-prov and the
   softness is cloth-phase — accepted into the cloth lane as the duvet's
   sim/shape ticket. C3's own note concedes the class is now legible
   ("ก้อนนิรนาม → ก้อนผ้านวม").
3. **"nightstand ทรงกระบอกตั้งพื้น"** → REFUTED third time (scene: floats
   311..481 in frame; the floor cylinder is `petcave`, a real target object).
   THREE blind critics have now misread this cluster — the recurrence is
   itself data: the right-lower cluster is illegible in clay and stays the
   materials phase's first legibility ticket.
4. r2b/r3 symptom check: **all four prior "anonymous mass / wrong-form"
   items now read correctly** (capsule ✓ foot blob ✓ waterfall desk ✓
   two-tier console ✓); เตียง = #1 above (clay).

Blockout scope: no open GEOMETRY defect names a mass out of place; every
standing critic item routes to materials/cloth/light or to the two owed
errands (shelf voids, slot) already queued for round 4.
