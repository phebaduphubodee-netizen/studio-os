# Bundle — trn002_blockout_r5c (round 5 frame of record: étagère, third build, first MEASURED one)

Render: `_private/…/renders/critique/critique-trn002_blockout_r5c/trn002_blockout_r5c.png`
Change from r5b: the corner column only. Owner C4: "ชั้นวางยังแตกต่างกัน" — and
at 6x the difference was STRUCTURAL: the target is an **open étagère** (a
continuous back panel to the top + three thin corner-wrapped boards + objects,
no front-face solid anywhere above the base) while both our builds were a
cabinet with window slots cut in it (fat boards 73-79 + a 407 solid cap that
does not exist).

**Why two builds got it wrong: both were built from eyeball reads of a 3x
crop (±30mm/edge). This one is fitted subpixel** (trn002_lines, two
independent seed windows agreeing ≤3mm):

- boards: z 1337..1385 / 1915..1970 / 2385..2456 (thickness = top-edge to
  light-strip envelope — an upper bound; the LED sits below the board)
- back panel top: **z 2918-2926 → REFUTES r2's coarse "top z~3030"** — and the
  fitted top aligns with the wardrobe bulkhead line (2920) across the room, a
  design consilience the coarse number hid.
- no solid above the base: cap DELETED, fat boards replaced.
- caveats declared: all fits on face plane y=-400 (A-swept); small n on three
  edges (vases occlude); b2_strip from the wide window only (n=5, but 6mm from
  r4's independent basis).

## Ladder state

- C1: col pair target|r5c — board levels land on the target's own lines;
  stranger sweep run (artwork, petcave, door zone hold).
- **C2 (Cowork, R7c): PENDING** — attach `trn002_blockout_r5c.png` + this
  PROMPT.md (program text now says étagère; supersedes the r5b ask).
- C3 (Gemini): filed. **No new geometry item.**

## C3-r5c triage (R7)

1. **เครื่องนอนแข็งทื่อ** → the R1-halted duvet class + pillow softness — owned
   by the CLOTH phase (halt of record in r5). Fourth critic to say it; the
   halt is the answer, per R8.
2. **"ชั้นบนสุด étagère ดูเป็นกล่องตัน"** → REFUTED with the file: above the
   base there is ONLY the 60-thick back panel and three boards ≤71 — no solid
   exists. What reads solid is the unlit top level (no shelf strips, no
   bounce) → LIGHT phase, same ticket as the slot void. Board thickness note:
   fitted envelope includes the strip offset; slimming is a materials-pass
   polish, not a rebuild.
3. **ม้านั่งใหญ่เทอะทะ** → REFUTED (recurring): top 420 and footprint are
   prov M — backprojected from the target's own bench in r2.
4. **petcave ไม่มีช่องโค้ง** → correctly located this time (not misbound —
   first critic to bind it right). The arch void is real target geometry in
   the frame corner; ACCEPTED into the styling/materials round as ticketed
   since r2b. Identity remains open (owner's call).
5. Clay-class notes (lamp detail, chair L-form, door grooves) → phase-correct,
   logged.
