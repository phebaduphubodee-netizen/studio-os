{
  "schema": "interior-ai/room-spec@0.2",
  "units": "metric",
  "_note": "GOLD-STANDARD sourceability binding example. Every SOURCED items[] piece carries an ffe_tag whose value is a ROLE tag in ffe-candidates.example.json (join key = the role `tag`, e.g. FFE-101 -> its selected:true candidate). BUILT-INS are FABRICATED (joiner-made) and carry NO ffe_tag. Run: python pipeline/scripts/sourceability_gate.py examples/scene-graph.example.json -> REVIEW, because the example picks are DRAFT (verified:false). Set the picks verified:true to reach PASS. ffe_tag is an OPTIONAL, backward-compatible room-spec@0.2 extension (old specs without it still load; it only gates once an ffe-candidates.json exists).",
  "ffe_candidates": "ffe-candidates.example.json",
  "room": {
    "type": "studio",
    "outline_mm": [[0, 0], [4500, 0], [4500, 4200], [0, 4200]],
    "ceiling_mm": 2700,
    "wall_thk_mm": 100
  },
  "items": [
    {"name": "sofa 3-seat", "kind": "sofa", "x": 300, "y": 3100, "w": 2100, "d": 950, "h": 850, "ffe_tag": "FFE-101"},
    {"name": "bed queen", "kind": "bed", "x": 2600, "y": 200, "w": 1650, "d": 2090, "h": 380, "ffe_tag": "FFE-201"}
  ],
  "builtins": [
    {"name": "tv media wall", "kind": "media_wall", "x": 0, "y": 1900, "w": 2800, "d": 300, "h": 2400}
  ]
}
